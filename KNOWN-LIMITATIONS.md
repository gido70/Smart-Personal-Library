# حدود معروفة — V0.7.3-candidate

ما لا يمكن أن يعمل مجانًا، وما لم يُختبر على جهاز حقيقي. هذا الملف مطلوب صراحة في `CLAUDE-REVIEW-PROMPT.md` وهو جزء لا يتجزأ من التسليم — القراءة قبل أي قرار نشر.

## 1. لا يوجد اختبار iPhone حقيقي — ولا يوجد ادّعاء بذلك

بيئة بناء هذا الكود لا تملك جهاز iPhone فعليًا ولا محرك WebKit (فقط Chromium عبر Playwright). كل ما ورد في `TEST-RESULTS.md` عن البند 7 هو **محاكاة** (حذف `Promise.withResolvers` من Chromium لتقليد محركًا لا يدعمها) — دليل قوي وموثّق على السبب الجذري والإصلاح، لكنه **ليس** بديلاً عن فتح الرابط فعليًا على iPhone وتجربة زر «شغّل التحليل المحلي المجاني» والصوت المجاني. الخطوة التالية المطلوبة قبل أي نشر: اختبار يدوي حقيقي على الجهاز الذي أنتج صور الدليل نفسه.

## 2. لا اتصال بمشروع Supabase الفعلي من هذه البيئة

تحققتُ مباشرة (`curl` إلى `nmbbahzzogspuuvpsxud.supabase.co`) أن بيئة التنفيذ التي أنتجت هذا الكود محجوبة عن الوصول لمشروعك على Supabase. هذا يعني أن كل اختبار يحتاج رفعًا أو حفظًا أو جلسة مصادقة حقيقية (البنود 1-4، 8، 13 في `TEST-RESULTS.md`) **لم يُختبر إطلاقًا هنا** — لا نجاحًا ولا فشلًا، بل غير قابل للتنفيذ في هذه الحاوية بالتحديد. هذا لم يكن قيدًا اخترته؛ هو واقع الشبكة في بيئة البناء، ويتوافق مع القيد الذي طلبته («لا تنشر شيئًا») لأنه يمنع أي تفاعل غير مقصود مع بياناتك الحقيقية.

## 3. لا OCR للصفحات المصوَّرة/الغلاف بلا طبقة نص

قيَّمت خيار OCR محلي داخل المتصفح (Tesseract.js WASM) ولم أنفّذه: الحزمة تزن عدة ميغابايتات إضافية (تتعارض مع مبدأ «التكلفة الصفرية» وسرعة التحميل على الهاتف)، ومعالجة كل صفحة كصورة عبر WASM على جهاز محمول تحتاج اختبار أداء حقيقي لم أستطع إجراءه (لا جهاز، لا وقت تنفيذ آمن للحكم على تجربة مستخدم مقبولة). بدل تنفيذ جزئي غير مُختبر وقد يبدو "يعمل" لكنه بطيء أو يُجمِّد المتصفح، اخترتُ الإفصاح الصادق: الصفحات المصوَّرة/الغلاف تُعرض برسالة واضحة تذكر أن الأمر يحتاج OCR غير متوفر في هذه النسخة المجانية، مع اقتراح تلقائي لأقرب صفحة فيها نص فعلي (ميزة جديدة في V0.7.3).

## 4. ترقية الحساب الدائم غير مُختبرة من طرف إلى طرف

آلية الترقية (`supabase.auth.updateUser({ email })`) مبنية وتحافظ على نفس هوية المستخدم (`auth.uid()`) نظريًا وحسب توثيق Supabase الرسمي لهذه الآلية، لكنها تحتاج لتفعيل "تأكيد البريد" (Confirm email) وإعداد SMTP فعلي على مشروعك — وهذه البيئة لا تملك وسيلة لاستقبال بريد إلكتروني حقيقي أو النقر على رابط تأكيد. **لم أختبر** أن الرابط يصل فعلًا، ولا أن الضغط عليه يُبقي كل كتب المستخدم كما هي بعد الترقية. يُنصَح باختبار هذا يدويًا (بريد اختباري حقيقي) قبل الإعلان عن هذه الميزة للمستخدمين.

## 5. البحث المحلي غير متاح للكتب المُحلَّلة قبل V0.7.3

ميزة «بحث داخل الكتاب» الجديدة تعتمد على حقل `pages_text` الذي لم يكن يُخزَّن في أي تحليل محلي سابق (V0.7/V0.7.1/V0.7.2). أي كتاب حُلِّل محليًا بنسخة أقدم لن يظهر له بحث حتى يُعاد تشغيل «التحليل المحلي» عليه من جديد — الواجهة تشرح هذا صراحة بدل إخفاء الميزة بصمت. كذلك، للكتب الأطول من 1200 صفحة، يُحذف الحقل كليًا (البحث غير متاح) لإبقاء حجم سجل التحليل المحفوظ في قاعدة البيانات معقولًا — هذا حد تصميمي واعٍ، لا خطأ.

## 6. منع التكرار الفعلي يحتاج تحققًا حيًا بعد النشر

لوحة مراجعة التكرارات الجديدة تتيح كشفًا وحذفًا يدويًا آمنًا للسجلات الموجودة فعلًا. المنع الحقيقي يعتمد على الفهرس الفريد الجزئي على `content_sha256` في `supabase/migrations/20260826_0002_spl_v07_zero_cost.sql`. هذه Migration نُفذت سابقًا بنجاح في مشروع Supabase الفعلي وفق نتيجة SQL التي قدّمها مالك المشروع، لكن منع الرفع المكرر نفسه لم يُختبر حيًا بعد من هذه البيئة.

## 7. لا اختبار صوت جهاز حقيقي (iOS/Android/Windows)

منطق اختيار الصوت العربي/الإنجليزي وتحميل `getVoices()`/`voiceschanged` هو نفسه من V0.7.1 (لم يتغيّر في V0.7.3 غير تسجيل الأخطاء)، وقد رُوجِع سابقًا لكن **لم يُختبر فعليًا بأصوات جهاز حقيقية** في أي من هذه الجلسات — الحاوية الآلية لا تملك محرك `speechSynthesis` بأصوات مثبَّتة. يبقى الاختبار اليدوي على جهاز حقيقي (كما جرى فعليًا مع V0.7.2 حسب `FAILURE-EVIDENCE.md`) هو الدليل الوحيد الموثوق لهذا الجزء تحديدًا.

## 8. حزم JavaScript كبيرة نسبيًا

تحذير بناء غير حرج: حزمتا `pdf-*.js` (~410 كيلوبايت مضغوطة) و`index-*.js` (~502 كيلوبايت) أكبر من الحد الموصى به (500 كيلوبايت) من Vite. لم يكن هذا ضمن نطاق طلب الإصلاح، ولم ألمسه — مذكور هنا للشفافية فقط، وهو موجود منذ V0.7.2 دون تغيير جوهري (نفس اعتمادية `pdfjs-dist`).
