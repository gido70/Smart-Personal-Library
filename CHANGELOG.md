# سجل الإصدارات

## غير منشور — رف النسخ المعرفية ووضوح المهام المدفوعة

- فصل الصفحة الرئيسية والمكتبة إلى «الكتب الأصلية» و«النسخ المعرفية»، مع حفظ اختيار المستخدم وبقاء البحث شاملًا للرفين.
- تحويل الأرشفة إلى حالة معرفة ظاهرة: يبقى الغلاف والفهرسة والتصنيف والخلاصة والتحليل والصوت والأسئلة، ويزال PDF الأصلي بعد تثبيت حالة الأرشيف.
- إعادة رفع الملف نفسه تستعيد سجل الكتاب ذاته ببصمة SHA-256 وتحافظ على النتائج المدفوعة دون إنشاء سجل جديد.
- إضافة مؤشر متحرك ثابت الظهور للتحليل والصوت والأسئلة والعينات، مع اسم المهمة ورسالة انتظار صريحة.
- حفظ المهمة المدفوعة ومعرّف طلبها محليًا ومتابعة نتيجتها بعد انقطاع الرد دون إعادة إرسال الطلب.
- إضافة قفل فوري في الواجهة إلى جانب إيصال منع التكرار الخادمي، حتى لا تنشأ طلبات أو خصومات متكررة بالنقر السريع.
- إصلاح حالة الصوت الجزئي: لا يعود وجود جزء واحد نجاحًا كاملًا؛ يحسب الخادم العدد المطلوب، يعيد استخدام الأجزاء المحفوظة، ويولّد الأجزاء الناقصة فقط.
- عرض تقدم صوتي حقيقي من إيصال المهمة مثل «تم حفظ 2 من 3»، مع اكتشاف التوقف وإتاحة «استكمال الصوت الناقص» دون شراء الأجزاء المحفوظة مرة أخرى.
- تثبيت نموذج الصوت المحسّن `gpt-4o-mini-tts-2025-12-15` وتعليمات راوٍ واحد لمنع تبدل الشخصية أو طبقة الصوت بين الأجزاء.
- توسيع الاختيار إلى ستة أصوات بعينات عربية، ومنع بدء الشراء الكامل لصوت جديد قبل سماع عينته.
- إظهار العدد المتوقع للأجزاء قبل الشراء، ثم حالة كل جزء على حدة (قيد الإنشاء/محفوظ) أثناء المهمة.
- تحديث فهرس الفكرة الشامل إلى الإصدار 1.9 بهذه القرارات.

## V0.7.3-candidate — إصلاح أعطال الهاتف الفعلي (لم يُنشر)

مبني على أدلة فشل حقيقية من V0.7.2 المنشورة فعليًا (`FAILURE-EVIDENCE.md`)، وليس تحسينًا نظريًا. راجع `CLAUDE-AUDIT-REPORT.md` للأسباب الجذرية و`TEST-RESULTS.md` للأدلة.

- إصلاح السبب الجذري الموحّد لعطل التحليل المحلي على iPhone وعطل الصوت المجاني: `Promise.withResolvers` (ES2024) غير مدعومة على Safari <17.4، تُستخدَم دون تحقق داخل `pdfjs-dist`؛ أُضيف polyfill مبكر (`src/lib/polyfills.ts`).
- الكتب الحقيقية تُعرض الآن كبطاقات غلاف بنفس جودة النماذج (`LiveBookCard`)، بدل سطور مسطّحة «UNKNOWN · uploaded».
- تعبئة رجعية للغة المكتشفة والعنوان الحقيقي إلى قاعدة البيانات بعد كل تحليل محلي.
- إزالة الترويسات/التذييلات المتكررة قبل توليد الخلاصة الاستخراجية والعناوين المرشحة.
- بحث محلي حرفي حقيقي داخل الكتاب («بحث داخل الكتاب») بأرقام صفحات ومقتطفات حقيقية.
- لوحة مراجعة تكرارات آمنة (بلا حذف تلقائي) للسجلات المكررة الموجودة فعلًا.
- اقتراح تلقائي لأقرب صفحة نصية عند صفحة غلاف/مصوَّرة بلا طبقة نص، مع تصريح صادق بالحاجة لـ OCR غير المتوفر.
- مسار ترقية الحساب المجهول إلى حساب دائم (بريد إلكتروني) بلا نقل ملكية ولا Migration.
- 9 اختبارات وحدة جديدة (23 → 32) تُعيد إنتاج سيناريوهي تكرار الترويسة والبحث المحلي من أدلة الفشل.
- تحديث اسم Cache إلى V0.7.3 لضمان وصول ملفات الإصدار الجديد إلى الهاتف بدل بقاء Cache V0.7.2.

## V0.7.1 — تجربة وظيفية بلا تكلفة

- فتح الكتاب المحفوظ من Supabase داخل القارئ دون اختياره من الجهاز مرة أخرى.
- منع تكرار الملف ببصمة SHA-256 بعد تطبيق Migration V0.7.
- حفظ الصفحة والعلامات بعد استعادة التقدم، دون استبدالها بالصفحة الأولى أثناء التحميل.
- تقسيم صوت الصفحة وإلغاء الطلبات القديمة حتى لا تُقرأ صفحة سابقة بعد الانتقال.
- منع استخدام صوت إنجليزي للنص العربي عند غياب صوت عربي مناسب.
- تحليل بنيوي محلي يحافظ على فواصل أسطر PDF لاكتشاف العناوين المرشحة.
- قفل OpenAI من الواجهة ومن وظيفة Supabase Edge افتراضيًا.
- قصر النسخة التجريبية على PDF لأن EPUB غير مدعوم بعد في القارئ والتحليل.
- جعل Migration ذرّية مع تحقق نهائي قبل اعتمادها.

## V0.5.0 — النموذج الوظيفي

- حفظ الكتب في Supabase Storage بدل فقدانها عند تغيير الصفحة.
- عزل جميع الجداول والسياسات بالبادئة `spl_` داخل مشروع الفلاح.
- إلزام الموافقة القانونية قبل الرفع.
- تحليل PDF عربي أو إنجليزي وإخراج عربي أو إنجليزي أو ثنائي اللغة.
- أسئلة موثقة من داخل الكتاب، وخلاصة صوتية مولدة بالذكاء الاصطناعي.
- الإبقاء على قارئ الأصل المحلي خيارًا منفصلًا لا يرفع الملف.

## V0.3.1 — إصلاح توافق PDF العربي

- جعل محرك PDF الأصلي في المتصفح هو وضع العرض الافتراضي للمحافظة على الخطوط والتشكيل وترتيب الحروف.
- إبقاء تقليب الصفحات المعتمد على PDF.js كخيار تجريبي منفصل، لا كقارئ أساسي.
- استمرار فتح الملف محليًا عبر رابط مؤقت في ذاكرة المتصفح دون رفعه إلى الخادم.
- إيقاف الانتقال إلى التحليل حتى ينجح اختبار ملف عربي فعلي.

## V0.3.0 — GitHub Pages

- تحويل النموذج من بيئة Sites الخاصة إلى تطبيق React ثابت قابل للنشر العام.
- الإبقاء على قارئ PDF المحلي وعدم رفع ملف الكتاب إلى خادم.
- إضافة نشر تلقائي عبر GitHub Actions.
- تجهيز هيكل مستقل لربط Supabase في الإصدار التالي.

## V0.2.0

- قارئ PDF محلي، تقليب الصفحات، التكبير، الوضع الليلي، وحفظ آخر صفحة.
# V0.6.0 — Free-first, cost-aware pilot

- فصل رفع الكتاب وحفظه عن أي استدعاء مدفوع لـ OpenAI.
- إضافة قراءة الصفحة بصوت الجهاز بالعربية أو الإنجليزية دون API.
- إضافة مسارات مستقلة للتحليل والترجمة والأسئلة والصوت الاحترافي مع تأكيد تكلفة.
- إضافة تقدير للكتاب الواحد ولتجربة عشرة كتب مقابل رصيد تجريبي قدره 10 دولارات.
- مراجعة المصطلحات الإنجليزية لتناسب مستخدمًا إنجليزيًا بدل محاكاة الواجهة العربية حرفيًا.
- إصلاح حفظ الملفات ذات الأسماء العربية باستخدام مسار تخزين تقني آمن مع إبقاء العنوان الأصلي ظاهرًا للمستخدم.

# V0.6.1 — Synchronized speech and cost safeguards

- جعل صوت الجهاز يعمل في وضع الكتاب المتزامن مع رقم الصفحة، مع زر استماع ظاهر أسفل الصفحة.
- تحميل أصوات المتصفح عبر `voiceschanged` والتنبيه إذا لم يتوفر صوت اللغة المختارة.
- إضافة تراجع يحذف الكتاب والملف إذا فشل حفظ إقرار الحقوق.
- تصحيح مبلغ 10 دولارات ليظهر كميزانية بداية يدوية لا كرصد حي لحساب OpenAI.
- تعزيز Edge Function بفحص ملكية صريح، ومنع إعادة التحليل والصوت، وحدود يومية محافظة.

# V0.6.2 — Device voice diagnostics

- اكتشاف لغة نص الصفحة تلقائيًا بدل ربطها بلغة الواجهة.
- منع محاولة قراءة الصفحات الفارغة أو التي لا تحتوي إلا رقم الصفحة.
- إظهار مسار Windows المباشر لتثبيت الصوت المطلوب عند غيابه.

# V0.6.3 — Automatic browser voice fallback

- اعتماد آلية الجسر نفسها: عند غياب صوت مطابق ظاهر، يختار المتصفح محرك النطق تلقائيًا بدل إيقاف القراءة.
- لا يستخدم هذا المسار OpenAI ولا Supabase ولا يخصم من رصيد API.
## V0.10.4 — Growing library and mobile reliability

- Remove the retired one-analysed-book server gate while preserving explicit confirmations and daily limits.
- Add exactly eleven top-level library gateways: ten Dewey classes plus modern and emerging topics, with second-level subdivisions only after selection.
- Allow one book to carry both a Dewey shelf and an optional modern topic without duplicating its record.
- Show a six-book display-only sample shelf when a new user's library is empty.
- Limit the active shelf to six originals and replace user-facing deletion with reversible archiving that preserves paid outputs.
- Distinguish awaiting, processing, completed and failed analysis states on book cards.
- Show daily usage, remaining allowance and the exact reset time before paid actions.
- Persist a moving paid-task marker across reloads and add an owner-scoped idempotency receipt migration to prevent repeated spending.
- Raise PDF upload size to 30 MB while preserving the independent 500-page limit and pre-charge validation.
- Render Android/Samsung covers through authenticated file downloads instead of fragile signed-URL range requests.
- Add a standard yellow notification bell and enable the mobile notifications destination.
- Expand the user guide for classification, archive, limits, progress, failure and no-charge messages.
- Improve dark-mode contrast for gold panels and newly added controls.
# V0.10.5-candidate

- جعل الكتب المكتملة صوتيًا في وضع التشغيل فقط، دون عرض إنشاء أو شراء أو اختيار صوت مرة أخرى.
- تحديث اختصار الهاتف إلى «تشغيل الصوت المحفوظ» عند اكتمال الصوت.
- إضافة اختبار دائم لحالة اكتمال الصوت.
- إضافة التصور التنفيذي الآمن لنسخة المشرف والمراجعين.
