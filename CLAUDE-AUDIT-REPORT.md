# تقرير تدقيق Claude — V0.7.3-candidate

التاريخ: 2026-08-26
المدخل: V0.7.2 المنشورة فعليًا على `gido70.github.io`، مع `FAILURE-EVIDENCE.md` وست صور تجربة حقيقية على كمبيوتر وiPhone.
المخرج: `Smart-Personal-Library-V0.7.3-Candidate.zip` — **لم يُنشر ولم يُدمَج ولم تُنفَّذ أي Migration**.

هذا التقرير يشرح **السبب الجذري** لكل عطل موثّق في `FAILURE-EVIDENCE.md`، وليس وصفًا للأعراض فقط، كما طلب `CLAUDE-REVIEW-PROMPT.md`.

---

## 1) السبب الجذري الموحّد لعطلَين مختلفين ظاهريًا: `Promise.withResolvers`

### الأعراض الموثقة
- التحليل المحلي يفشل على iPhone: `undefined is not a function (near '...e of t...')`.
- الصوت المجاني يفشل على الهاتف: «تعذر استخراج نص هذه الصفحة للصوت المجاني».
- كلاهما ينجح على الكمبيوتر (Chromium/Chrome).

### التشخيص
كلا المسارين ينفذان نفس سلسلة استدعاء PDF.js:
`document.getPage(n)` → `page.getTextContent()`، في `src/lib/localAnalysis.ts` (`runLocalStructuralAnalysis`) وفي `src/Reader.tsx` (`speakPage`).

فحصت الحزمة المثبَّتة فعليًا (`pdfjs-dist@5.7.284`، يحلّها `package.json`'s `^5.4.149`) بالبحث المباشر (`grep`) عن استخدام `Promise.withResolvers()`، وهي دالة ساكنة أضيفت إلى JavaScript في **ES2024** ودعمها Safari فقط ابتداءً من **17.4** (مارس 2024):

| الملف | عدد الاستخدامات |
|---|---|
| `node_modules/pdfjs-dist/build/pdf.mjs` | 26 |
| `node_modules/pdfjs-dist/build/pdf.worker.min.mjs` | 3 |
| `node_modules/pdfjs-dist/legacy/build/pdf.mjs` | 29 |

الاستخدام غير مشروط (لا يوجد أي تحقق من توفر الدالة قبل استدعائها) وداخل نمط "deferred promise" المستخدم في مسارات أساسية (تحميل المستند، تحميل الصفحة، الـ streams الداخلية). أي iPhone على إصدار Safari أقدم من 17.4 سيجد `Promise.withResolvers === undefined`، فيرمي WebKit استثناء يقرأ تمامًا كما في الدليل: رسالة "undefined is not a function" مع مقطع سياق مشوَّه (`near '...e of t...'`) — وهذا نمط رسائل WebKit المعروف عند تصغير الكود (minification) وعدم توفر اسم دالة واضح.

كذلك تأكدت أن نسخة "legacy" من `pdfjs-dist` **لا** تتجنب هذا — فهي أيضًا تستخدم الدالة 29 مرة، لأن "legacy" في `pdfjs-dist` يعني بدائل لواجهات DOM/runtime قديمة، وليس تنزيلًا نحويًا (syntax downleveling) لميزات ES حديثة. الاعتماد على تبديل الحزمة وحده لم يكن سيصلح المشكلة.

### لماذا هذا يفسر **العطلين معًا**
- في `localAnalysis.ts`: `document.getPage`/`getTextContent()` يُستدعيان مباشرة لكل صفحة أثناء التحليل البنيوي.
- في `Reader.tsx::speakPage`: نفس الاستدعاءين يُستدعيان لاستخراج نص الصفحة المفتوحة قبل القراءة الصوتية، **وكانا ملفوفين بـ `catch` عام واحد** يعرض دائمًا نفس الرسالة الثابتة «تعذر استخراج نص هذه الصفحة للصوت المجاني» — بصرف النظر عن سبب الفشل الحقيقي. هذا الـ `catch` كان يُخفي (يبتلع) استثناء `Promise.withResolvers is not a function` بالضبط، فتظهر رسالة عامة غير تشخيصية بدل الخطأ الحقيقي.

### الإصلاح (V0.7.3)
أضفت polyfill أدنى-تدخل، متوافق مع المواصفة، بلا أي اعتمادية خارجية، في ملف جديد `src/lib/polyfills.ts`، يُستورَد كأول سطر في `src/main.tsx` (قبل React وقبل أي استيراد آخر) — أي قبل أي `import("pdfjs-dist")` ديناميكي ممكن أن يحدث لاحقًا نتيجة تفاعل المستخدم:

```ts
if (typeof Promise.withResolvers !== "function") {
  Promise.withResolvers = function withResolvers() {
    let resolve, reject;
    const promise = new Promise((res, rej) => { resolve = res; reject = rej; });
    return { promise, resolve, reject };
  };
}
```

كذلك جعلت `catch` الخاص بـ `speakPage` في `Reader.tsx` يسجّل الاستثناء الحقيقي في console (`console.error`) بدل ابتلاعه بصمت، حتى لو تكرر عطل مختلف مستقبلًا يكون قابلًا للتشخيص من DevTools مباشرة، لا رسالة عامة فقط.

### التحقق (محاكاة، وليس جهازًا حقيقيًا — انظر القسم 8 وTEST-RESULTS.md)
لا يتوفر متصفح WebKit حقيقي في بيئة التنفيذ هذه (`/opt/pw-browsers` يحوي فقط نسخ Chromium)، ولا يوجد جهاز iPhone فعلي. بدل الاكتفاء بالتخمين، بنيت محاكاة صارمة عبر Playwright+Chromium: حذف `Promise.withResolvers` من الصفحة قبل تحميلها (`page.addInitScript`) لمحاكاة محرك لا يدعمها، ثم تشغيل *نفس* سلسلة الاستدعاء الحقيقية (`getDocument → getPage → getTextContent`) من نفس نسخة `pdfjs-dist` المثبتة على ملف PDF حقيقي:

| السيناريو | النتيجة |
|---|---|
| Chromium عادي (الدالة متوفرة أصلًا) | ✅ نجاح — استخرج النص |
| محاكاة WebKit قديم، **بلا** الإصلاح | ❌ `Promise.withResolvers is not a function` — يعيد إنتاج فئة الخطأ في الدليل تمامًا |
| محاكاة WebKit قديم، **مع** الإصلاح المُركَّب في V0.7.3 | ✅ نجاح — استخرج النص رغم غياب الدالة الأصلية |

هذا يثبت السبب الجذري ويثبت أن الإصلاح يعمل، **لكنه محاكاة لا تُغني عن اختبار iPhone فعلي** — راجع `TEST-RESULTS.md` البند 7 و`KNOWN-LIMITATIONS.md`.

كذلك تحققت أن حزمة الإنتاج الفعلية (`npm run build` → `dist/`) تُركّب الإصلاح فعليًا قبل أي كود آخر: حمّلت `dist/index.html` في نفس المحاكاة (الدالة محذوفة مسبقًا) وتأكدت أن `Promise.withResolvers` أصبحت موجودة مجددًا فور تنفيذ الحزمة، بلا أي أخطاء JS غير ملتقطة أثناء التحميل.

---

## 2) الكتب الحقيقية تظهر كسطور مسطّحة «UNKNOWN · uploaded»

### التشخيص
لم يكن هذا خللًا جديدًا من V0.7.1/V0.7.2 — كود `Library` في `src/App.tsx` كان لا يزال يعرض `pilotBooks` (الكتب الحقيقية) كأزرار مسطّحة بسيطة (`<button><i>▤</i><span>...`)، بينما الكتب النموذجية الثلاثة (`books` الثابتة) تُعرض عبر `BookCard`/`BookCover` بجودة بصرية كاملة (غلاف بلون، إطار، عنوان). لم يمسّ Codex هذا الجزء إطلاقًا في مراجعته لـ V0.7.1 (موثّق في `CODEX-REVIEW-V0.7.1.md`، لا يذكره).

سبب ظهور «UNKNOWN» تحديدًا: `spl_books.source_language` تُنشأ بالقيمة الافتراضية `'unknown'` عند الرفع، ولا شيء في المسار المجاني (التحليل المحلي عبر PDF.js) كان يكتب اللغة المكتشفة رجوعًا إلى قاعدة البيانات — فقط الدالة السحابية المدفوعة (`spl-ai`, المُقفلة الآن بشكل متعمّد في وضع التكلفة الصفرية) كانت تكتب `source_language` بعد التحليل. والنتيجة: كل كتاب حقيقي يبقى «UNKNOWN» إلى الأبد ما دامت الميزات المدفوعة مقفلة، وهذا بالتحديد وضع V0.7 «التجربة المجانية».

### الإصلاح
- **بطاقة حقيقية بنفس جودة النماذج**: مكوّن جديد `LiveBookCard` في `src/App.tsx` يستخدم نفس أصناف CSS (`book-card`, `book-cover`) التي تستخدمها النماذج الثلاثة — لا هوية بصرية جديدة، بل إعادة استخدام الموجود فعلاً. الغلاف يُولَّد محليًا (لا صورة، لا API مدفوعة) بلون حتمي مُشتق من عنوان الكتاب نفسه (دالة `coverToneFor`، تجزئة بسيطة إلى أحد الألوان الثلاثة الموجودة أصلًا: emerald/navy/gold) — نفس الكتاب يحصل دائمًا على نفس اللون.
- **نص فرعي صادق بدل «UNKNOWN»**: عندما لا تكون اللغة معروفة بعد، يُعرض «لم تُحدَّد بعد» (Not detected yet) بدل قيمة تقنية خام بالإنجليزية الكبيرة.
- **حالة صادقة بدل `status` الخام**: «محفوظ — بانتظار التحليل المحلي» بدل كلمة `uploaded` وحدها.
- **تعبئة رجعية (backfill) للغة والعنوان**: دالة جديدة `backfillBookMetadataFromLocalAnalysis` في `src/lib/library.ts` تُستدعى تلقائيًا في نهاية كل تشغيل ناجح للتحليل المحلي (`runLocalStructuralAnalysis` في `src/lib/localAnalysis.ts`)، وتكتب `source_language` المكتشفة فعلاً إلى `spl_books` — **فقط** إذا كانت لا تزال `unknown` (لا تكتب فوق قيمة موجودة من مسار آخر)، وتُحدّث العنوان فقط إذا وجدت عنوانًا حقيقيًا في بيانات PDF الوصفية (`pdf_metadata.Title`) يختلف عن العنوان الحالي. هذا يعمل **اليوم**، دون انتظار أي Migration، لأن العمودين `source_language`/`title` موجودان أصلًا في المخطط الأساسي (V0.5)، وسياسة RLS الحالية (`for all ... using(user_id = auth.uid())`) تسمح للمستخدم بتحديث صفوفه هو فقط.

### دليل بصري (محاكاة عرض، بلا اتصال حي بـ Supabase — انظر القسم 8)
أعدت بناء بطاقة فعلية بنفس صنف CSS المُشغَّل فعليًا في حزمة الإنتاج، بعنوان الكتاب نفسه من الدليل («كيف استخدم كلاود؟»)، وصوّرتها بمتصفح حقيقي: النتيجة بطاقة غلاف ملوَّنة بعنوان واضح ووسم «كتابك» وحالة صادقة — بنفس نمط بطاقات النماذج الثلاثة تمامًا، بدل السطر المسطّح القديم.

---

## 3) خلاصة استخراجية غير صالحة — تكرار ترويسة واحدة عبر صفحات 60-67

### التشخيص
صورة الدليل (`03-desktop-local-analysis-output.png`) تُظهر بوضوح أن «الخلاصة الاستخراجية» أعادت نفس السطر («Claude 5: كزميل عمل 100 خطوة لإتقان») ثماني مرات، مرة لكل صفحة من 60 إلى 67. السبب: `buildExtractiveSummary` في `src/lib/textAnalysis.ts` كانت تُغذَّى مباشرة بنص كل صفحة **بلا أي تصفية** لترويسات/تذييلات متكررة (running headers/footers) تظهر عادة أعلى كل صفحة في كتب PDF منسقة — فيصادف أن هذا السطر يحقق أعلى تقييم (score) لأنه يحتوي كلمات متكررة كثيرًا في الكتاب (اسم الفصل نفسه)، فيُختار من كل صفحة يظهر فيها.

### الإصلاح
دالة جديدة `stripRepeatedLines(pages)` في `src/lib/textAnalysis.ts`: تفحص أول وآخر 3 أسطر من كل صفحة (حيث تعيش الترويسات/التذييلات عادة، لا متن النص)، تُطبّع الأرقام (`12`/`13` → نفس المفتاح) لتلتقط ترقيم الصفحات المتغيّر، وتعتبر أي سطر **يتكرر على 4 صفحات على الأقل أو أكثر من ثلث الصفحات** (أيهما أصغر) نصًا شكليًا (boilerplate) يُحذف قبل توليد عناوين الفصول المرشحة والخلاصة الاستخراجية فقط — لا تُحذف من عدّ الكلمات/الصفحات الحقيقي كي تبقى الإحصاءات (عدد الكلمات، إلخ) دقيقة.

**اختبار وحدة حقيقي وليس فحصًا بصريًا فقط**: أضفت في `scripts/test-text-analysis.mjs` سيناريو يُعيد إنتاج حالة الدليل بالضبط (سطر ترويسة يتكرر على 4 من 5 صفحات بينما تحمل كل صفحة جملة حقيقية مختلفة)، ويتحقق أن: (أ) الترويسة تُحذف من كل الصفحات التي لا تحتاجها، (ب) كل جملة حقيقية مختلفة تبقى، (ج) الخلاصة الاستخراجية الناتجة فعليًا **لا تحتوي** الترويسة كجملة مُختارة. 32/32 اختبارًا ناجحًا (كانت 21 في V0.7.1 حسب `CODEX-REVIEW-V0.7.1.md`).

---

## 4) الصوت المجاني: صفحة غلاف/مصوَّرة بلا طبقة نص

### التشخيص
كان الكود (منذ إصلاح Codex في V0.7.1) يكتشف بالفعل الصفحة الفارغة (أقل من 10 أحرف قابلة للقراءة) ويعرض رسالة صادقة تذكر أن الصفحة المصوَّرة تحتاج OCR — هذا جزء صحيح وسليم. لكن لم يكن هناك أي مسار عملي للمستخدم: لا اقتراح لأقرب صفحة فيها نص فعلي، ولا زر انتقال — تمامًا كما يطلب البند (د) في `CLAUDE-REVIEW-PROMPT.md` («اقترح الانتقال لأول صفحة نصية»).

### الإصلاح
دالة `findNextTextPage` جديدة في `src/Reader.tsx`: عند اكتشاف صفحة بلا نص كافٍ، تبحث تلقائيًا إلى الأمام (حتى 25 صفحة) ثم إلى الخلف عن أقرب صفحة فيها طبقة نص حقيقية (تستخدم نفس `getPage`/`getTextContent` المُصلَحين أعلاه)، وتعرض للمستخدم زر «انتقل إليها» يقفز مباشرة لتلك الصفحة. البحث يُلغى فورًا إذا غيّر المستخدم الصفحة أو أوقف القراءة أثناءه (عبر رمز جيل الطلب `speechGenerationRef` نفسه المستخدم أصلًا لمنع تكرار صوت صفحة قديمة).

**لم أنفّذ OCR فعليًا** — قيّمت الخيار (Tesseract.js WASM يزن عدة ميغابايتات، ويحتاج معالجة صور كل صفحة داخل المتصفح، وهذا عبء أداء وحجم غير واقعي لتجربة "تكلفة صفرية" على الأجهزة المحمولة بلا اختبار حقيقي) وقررت الإفصاح الصادق بدل التنفيذ الجزئي غير المُختبر، تمامًا كما ينص معيار القبول: «صرّح بأن الصفحة المصوّرة تحتاج OCR ولا تدّع عمل الصوت عليها» — هذا موثق بوضوح في `KNOWN-LIMITATIONS.md`.

---

## 5) عدم وجود بحث حقيقي داخل الكتاب (البند هـ.1)

### التشخيص
لم يكن يوجد أي مسار مجاني صادق لـ«سؤال الكتاب» غير المسار المدفوع المقفل. البند هـ يطلب صراحة أحد خيارين صادقين: بحث محلي حرفي، أو إبقاء السؤال الذكي مقفلًا بتفسير واضح. الخيار الثاني كان موجودًا (القفل + الرسالة)، لكن الخيار الأول (البحث المحلي) لم يكن موجودًا إطلاقًا.

### الإصلاح
- وسّعت `LocalStructuralAnalysis` (في `src/lib/textAnalysis.ts`) بحقل اختياري جديد `pages_text`: نص كل صفحة كاملًا (بحد أقصى 6000 حرف للصفحة، ويُحذف الحقل كليًا للكتب الأكبر من 1200 صفحة لإبقاء حجم السجل المحفوظ معقولًا) — يُستخدم فقط للبحث المحلي، ولا يُعرض كـ"تحليل بالذكاء الاصطناعي" ولا يُرسل لأي خدمة.
- دالة نقية جديدة `searchInsideBook(pagesText, query)`: مطابقة نصية حرفية (substring)، بعد إزالة التشكيل العربي فقط لزيادة نسبة المطابقة، تُعيد رقم الصفحة الحقيقي ومقتطفًا حقيقيًا من النص المحيط بالمطابقة — لا تفسير للمعنى، لا تصنيف بالأهمية، بحث حرفي بحت ومُسمّى بوضوح «بحث داخل الكتاب (محلي)» في الواجهة، منفصل تمامًا عن لوحة «اسأل الكتاب» المدفوعة المقفلة.
- 5 اختبارات وحدة جديدة تتحقق أن البحث يعيد أرقام صفحات صحيحة، ومقتطفات تحتوي فعلاً كلمة البحث، وأن غياب النتائج أو غياب `pages_text` (كتاب حُلِّل قبل هذه الميزة) يُعيد `[]` بلا رمي استثناء — مع رسالة صادقة في الواجهة تطلب إعادة تشغيل التحليل بدل فشل صامت.

---

## 6) تكرار السجلات المرفوعة (نسخ متعددة من «كيف استخدم كلاود؟»)

### التشخيص
كان الحل الموجود دفاعًا من طرفين (تحقق hash في العميل + فهرس فريد جزئي في قاعدة البيانات). Migration الـ V0.7 التي تضيف `content_sha256` نُفذت سابقًا بنجاح في مشروع Supabase الفعلي وفق نتيجة SQL التي قدّمها مالك المشروع؛ وما يزال اختبار المنع حيًا بعد النشر مطلوبًا. لم تكن هناك واجهة مراجعة للسجلات المكررة الموجودة فعلًا على الخادم، رغم أن البند 3 من `CLAUDE-REVIEW-PROMPT.md` يطلبها صراحة («لا تحذف تلقائيًا… قدّم تقريرًا أو شاشة مراجعة»).

### الإصلاح — بلا حذف تلقائي إطلاقًا
- `groupDuplicateBooks(books)` جديدة في `src/lib/library.ts`: تُجمّع كتب المستخدم (المُستَرجَعة أصلًا عبر RLS، أي كتبه هو فقط) حسب `content_sha256` عندما يتوفر (مطابقة محتوى **مؤكدة**)، وكحل احتياطي فقط للسجلات الأقدم بلا hash، حسب العنوان المُطبَّع + حجم الملف الدقيق (مطابقة **غير مؤكدة**، مُعلَّمة بوضوح ومُفرَّقة بصريًا بلون مختلف).
- مكوّن جديد `DuplicateReviewPanel` في صفحة المكتبة: يعرض كل مجموعة، ويطلب **نقرتين صريحتين** («حذف هذه النسخة» ثم «نعم، احذف») قبل استدعاء `rollbackPilotBook` الموجودة أصلًا (تحذف من قاعدة البيانات والتخزين معًا، RLS-scoped). لا حذف تلقائي، ولا حذف بنقرة واحدة، ولا حذف لآخر نسخة متبقية بمنطق خاص — المستخدم من يقرر أي نسخة يحذف.
- **ملاحظة صادقة موثقة**: لأن Migration الـ V0.7 نُفذت سابقًا، يُفترض أن تظهر الرفعات الجديدة ذات hash كمطابقات «مؤكدة». السجلات القديمة التي سبقت تخزين hash قد تظل «غير مؤكدة» حتى المراجعة أو التعبئة الرجعية.

---

## 7) الحسابات والأجهزة — لا مكتبة دائمة متعددة الأجهزة

### التشخيص
الهوية المجهولة (`supabase.auth.signInAnonymously()`) مرتبطة بمتصفح/جهاز واحد فقط، والتحذير النصي وحده لم يقترن بأي مسار عملي للترقية.

### الإصلاح
استخدمت آلية Supabase الأصلية للترقية من مجهول إلى دائم: `supabase.auth.updateUser({ email })` — **وليس** إنشاء حساب منفصل. هذا يحافظ على نفس `auth.uid()` للمستخدم المسجَّل دخوله بالفعل كمجهول، فكل صف موجود في `spl_books`/`spl_analyses`/... (المُقيَّد أصلًا بـ RLS حسب هذا الـ uid) يصبح متاحًا من الحساب الدائم **بلا أي Migration وبلا أي نقل ملكية** — تمامًا كما يشترط البند (و) («لا تنقل ملكية سجلات المستخدم… دون Migration وخطة تحقق»): هنا لا نقل ملكية إطلاقًا لأن الهوية (uid) لم تتغيّر، فقط أُضيف إليها بريد.
- دالتان جديدتان في `src/lib/supabase.ts`: `upgradeAnonymousSessionToEmail(email)` و`isCurrentSessionAnonymous()`.
- لوحة UI جديدة `AccountUpgradePanel` أعلى قائمة الكتب الحقيقية، تشرح بوضوح أن الترقية لا تنقل بيانات وأنها تحتاج تأكيدًا عبر رابط بريدي قبل أن تصبح فعلية.

**غير مُختبَر فعليًا من طرف إلى طرف** — هذا يحتاج إعدادات SMTP/تأكيد بريد مُفعَّلة على مشروع Supabase الحقيقي، ولا توجد للبيئة التي أنتجت هذا الكود أي وسيلة لاستقبال بريد إلكتروني حقيقي أو تأكيد الرابط. موثّق بوضوح في `KNOWN-LIMITATIONS.md` كـ BLOCKED/UNTESTED — الكود موجود ومبني بعناية، لكن لم أدّعِ أنه "يعمل" دون دليل.

---

## 8) لماذا لم أختبر مقابل Supabase الحقيقي (توضيح صادق)

بيئة التنفيذ التي أنتجت هذا الكود **لا تملك اتصال شبكة** بمشروع Supabase الفعلي (`nmbbahzzogspuuvpsxud.supabase.co`) — تحققت من هذا مباشرة (`curl` انتهى بمهلة/رفض اتصال). هذا يعني أنني **لم أستطع**، ولم أحاول، رفع كتاب حقيقي أو تسجيل دخول مجهول فعلي أو تنفيذ أي عملية شبكة حقيقية ضد مشروعك أثناء بناء هذه النسخة — وهذا متسق تمامًا مع القيد الصارم «لا تنشر شيئًا». كل الاختبارات القابلة للتنفيذ في هذه البيئة (وحدات، بناء، محاكاة WebKit، فحص عرض بصري بلا Supabase) **نُفِّذت فعليًا** ونتائجها موثقة بصدق في `TEST-RESULTS.md`؛ وكل ما يحتاج اتصالًا حيًّا بمشروعك الفعلي أو جهاز iPhone حقيقي مُعلَّم بوضوح `BLOCKED` وليس `PASS` مزيّف.

---

## ملخص: ما الذي **لم** يتغيّر عمدًا

- منطق تسجيل الموافقة القانونية بعد الرفع (`startProcessing` في `App.tsx`): كان يحتوي أصلًا على تراجع (rollback) صحيح عند فشل تسجيل الموافقة (يحذف الكتاب المرفوع بدل تركه سجلًا يتيمًا)، من عمل Codex في V0.7.1. راجعته وأجده سليمًا كما هو — لم أغيّره.
- أمان الدالة السحابية `spl-ai/index.ts` (فحص ملكية صريح، RLS عبر مفتاح anon + Authorization مُمرَّر، قفل افتراضي `SPL_PAID_AI_ENABLED`): سليم كما هو، لم يتغيّر.
- الهوية البصرية العامة: لم تتغيّر — كل إضافة بصرية جديدة (بطاقات الكتب الحقيقية، لوحة المراجعة، لوحة الترقية) تعيد استخدام نفس متغيرات الألوان (`--ink`, `--soft`, `--line`, `--card`, `--gold`, `--red`) ونفس أصناف `book-card`/`book-cover` الموجودة أصلًا.
