# تقرير تدقيق Claude — V0.6.3 → V0.7 Zero-Cost Functional Pilot

تاريخ التدقيق: يُقرأ من نظام الجلسة وقت التسليم. هذا تدقيق كود ثابت (static audit) بقراءة كل ملف مرفق فعليًا، وليس تخمينًا. حيث لم أستطع التحقق فعليًا (لأن ذلك يتطلب اتصالًا حيًا بمشروع Supabase الخاص بك وبيانات اعتماد لا تملكها هذه البيئة)، أذكر ذلك صراحة بدل الادعاء بأنني اختبرته.

---

## أ. الكتاب لا يبقى متصلًا بالقارئ

**السبب الجذري المؤكد بقراءة الكود:**
`src/App.tsx` (السطر ~335 في V0.6.3) يُصيّر `<Reader rtl={rtl} />` بلا أي خاصية أخرى — لا `book`، لا `signedUrl`، لا `bookId`. وفي المقابل `src/Reader.tsx` القديم لا يملك حتى تعريف Prop لاستقبال كتاب محفوظ؛ مصدره الوحيد للملف هو `<input type="file">` محلي (`openFile`). زر «فتح القارئ والصوت المجاني» داخل `PilotWorkspace` (السطر ~900) كان ينادي `onOpenReader` التي تكتفي بـ `setView("reader")` دون تمرير أي بيانات عن الكتاب. لا توجد أي دالة اسمها `createBookSignedUrl` في `src/lib/library.ts` الأصلي — لذلك لم يكن هناك مسار برمجي يربط سجل `spl_books` بقارئ PDF إطلاقًا. هذا يفسر تمامًا كل الأعراض الثلاثة التي وصفتها (طلب اختيار الملف من جديد، تكراره عند كل انتقال، وعدم بقاء الكتاب مفتوحًا).

**الإصلاح المنفَّذ:**
- أضفت `createBookSignedUrl(storagePath, expiresIn)` في `library.ts`.
- أضفت لـ`Reader` خاصية جديدة `savedBook` (id + title + storagePath)، ومسار تحميل منفصل تمامًا عن مسار الملف المحلي: يطلب Signed URL، ثم يحمّل PDF.js عبر `getDocument({ url })` مباشرة بدل قراءة بايتات ملف محلي.
- `App.tsx` أضاف `readerBook` state مستقل: يُملأ فقط عند الضغط على «افتح القارئ والصوت المجاني» من صفحة الكتاب، ويُفرَّغ عند إغلاق القارئ أو الدخول إليه من التنقل العام (لتفادي بقاء كتاب سابق عالقًا بالخطأ).
- الصفحة ورقم الصفحة والعلامات المرجعية لكتاب محفوظ تُحفظ الآن في جدول `spl_reading_progress` (موجود في المخطط الأصلي ولم يكن مستخدَمًا إطلاقًا) بدل `localStorage`، فتبقى متزامنة عبر الجلسات على نفس المتصفح.
- عند انتهاء صلاحية الرابط الموقَّع (Signed URL)، تظهر رسالة صريحة مع زر «تجديد الرابط» يعيد طلب رابط جديد بدل فشل صامت.

**الحدود المعروفة:** لم أستطع فتح كتاب فعلي من مشروع Supabase الحقيقي الخاص بك من هذه البيئة (لا اتصال ولا بيانات اعتماد هنا)، فالتحقق من نجاح `getDocument({url: signedUrl})} عمليًا يبقى على عاتقك كخطوة اختبار بشري أخيرة موثّقة في `TEST-RESULTS.md`.

---

## ب. عدم وضوح مكان الكتاب

**السبب الجذري:** `listPilotBooks()` كانت تُستدعى مرة واحدة فقط عند التحميل (`useEffect([])`) بدون معالجة خطأ ظاهرة (`.catch(() => undefined)`) وبدون حالة تحميل/فراغ حقيقية؛ وبطاقة الكتاب في `Library` كانت تعرض فقط العنوان واللغة والحالة، ولا تفتح "سجلًا ثابتًا" شاملًا — كانت تنقل مباشرة إلى `PilotWorkspace` الذي بدوره ركّز فقط على مسارات الدفع، دون عرض منظم لبيانات الكتاب نفسه (الحجم، تاريخ الرفع، مسار التخزين، حالة الموافقة القانونية).

**الإصلاح المنفَّذ:**
- أضفت حالة تحميل/فراغ/خطأ حقيقية وواضحة في `Library` (`booksLoading`, `booksError`, رسالة صريحة + زر إعادة محاولة).
- أضفت بطاقة «بيانات الكتاب» ثابتة أعلى `PilotWorkspace` تعرض: العنوان، اسم الملف الأصلي، الحجم، تاريخ الرفع (مُنسَّق)، الحالة، لغة المصدر، لغة المخرجات، مسار التخزين، وحالة الموافقة القانونية (بقراءة فعلية من `spl_legal_consents` عبر دالة جديدة `getLegalConsentStatus`).
- بعد أي Refresh تُستدعى `listPilotBooks()` من جديد تلقائيًا (لم يتغيّر مبدئيًا، لكنه أصبح موثّقًا ومغطى باختبار قبول صريح لأن الاعتماد الوحيد على mount-effect كان يعمل أصلًا، والمشكلة الحقيقية كانت غياب واجهة تُظهر الفشل إن حدث).

---

## ج. تكرار رفع الكتاب نفسه

**السبب الجذري:** لا يوجد أي حساب لبصمة محتوى الملف قبل الرفع؛ `uploadPilotBook` تنشئ `crypto.randomUUID()` جديدًا لكل استدعاء وترفع دائمًا نسخة جديدة إلى `spl-books`، بصرف النظر عن كون نفس الملف مرفوعًا من قبل. جدول `spl_books` لا يملك عمود بصمة أصلًا (`content_sha256` غير موجود في migration الحالي)، ولا قيدًا فريدًا يمنع التكرار على مستوى قاعدة البيانات.

**الإصلاح المنفَّذ:**
- أضفت `hashFile()` في `src/lib/textAnalysis.ts` تحسب SHA-256 في المتصفح عبر `crypto.subtle.digest`، تُستدعى قبل أي رفع.
- `uploadPilotBook` أصبحت تتحقق أولًا من وجود كتاب بنفس `content_sha256` لنفس المستخدم (RLS يحصر النتيجة أصلًا في كتب المستخدم الحالي)؛ إن وُجد تُعيد السجل الموجود مع `deduped: true` دون رفع أو إدراج جديد.
- أضفت migration منفصلاً (`20260826_0002_spl_v07_zero_cost.sql`، **غير منفَّذ**) يضيف العمود `content_sha256` وفهرسًا فريدًا جزئيًا `(user_id, content_sha256)` حيث القيمة ليست NULL — هذا يمنع التكرار على مستوى قاعدة البيانات أيضًا كخط دفاع ثانٍ، وليس فقط في الواجهة.
- **قبل تنفيذ الـMigration**: الكود يتعامل بأمان مع غياب العمود — إن فشل الاستعلام أو الإدراج بسبب عمود غير موجود، يتجاهل خطوة الكشف عن التكرار بصمت (مع تحذير في console فقط) ويكمل الرفع بالسلوك القديم، حتى لا يكسر التطبيق قبل مراجعتك وتنفيذك للـMigration.

---

## د. الصوت يقرأ العربية بصوت إنجليزي ويكرر جملة/صفحة

**السبب الجذري (مثبَت بقراءة `speakPage` في V0.6.3):**
1. **لا تقسيم للنص**: الصفحة كاملة تُمرَّر كـ`SpeechSynthesisUtterance` واحد. المتصفحات (خصوصًا Chrome) لها سقف زمني/طولي غير موثق رسميًا لكل Utterance؛ نصوص طويلة تُسبب تعليقًا أو انقطاعًا أو — في تطبيقات الملاحظات الميدانية الشائعة — تكرار المقطع الأخير قبل التوقف.
2. **لا Queue ولا Generation Token**: لا يوجد أي آلية تمنع Callback من Utterance قديم (`onend`) من التأثير على حالة جديدة بعد تغيير الصفحة أو الضغط على زر آخر بسرعة.
3. **اختيار الصوت غير حاسم**: الكود (نسخة V0.6.3 المرفقة فعلًا أكثر تقدمًا مما راجعته سابقًا؛ فهي تكتشف اللغة من نص الصفحة لا من لغة الواجهة، وتنتظر `voiceschanged`) — لكنه يبقى فجوة حرجة واحدة: إن لم يوجد صوت مطابق للعربية على الجهاز، الكود **لا يمتنع** عن النطق؛ فقط يترك `utterance.voice` بلا تعيين ويُكمل النداء بـ`utterance.lang="ar-SA"` فقط. على أجهزة بلا حزمة صوت عربي، يستخدم بعض المتصفحات الصوت الافتراضي (غالبًا إنجليزي) لنطق الحروف العربية حرفيًا — وهذا بالضبط عرض «يقرأ العربية بصوت إنجليزي» الذي وصفته.

**الإصلاح المنفَّذ:**
- تقسيم نص الصفحة إلى جمل عبر تعبير نمطي يدعم علامات الوقف العربية والإنجليزية (`. ! ؟ ?`)، ثم تجميعها في مقاطع لا تتجاوز حدًا آمنًا للطول (≈220 حرفًا) — كل مقطع Utterance مستقل.
- طابور تشغيل صريح (`speechQueueRef`) بمقطع واحد نشط في كل لحظة؛ `onend` يُطلق المقطع التالي فقط.
- Generation Token (`speechGenerationRef`, رقم يتصاعد): كل استدعاء `speakPage` جديد أو تغيير صفحة أو إغلاق أو Stop يزيد الرقم؛ أي Callback من جيل قديم (`onend`/`onerror`) يتجاهل نفسه فورًا فيتحقق من تطابق الجيل قبل تشغيل المقطع التالي. هذا يمنع "التكرار" الناتج عن تداخل أحداث قديمة وجديدة.
- **رفض صريح بدل نطق إنجليزي مضلِّل**: إن كانت اللغة المكتشفة عربية ولم يوجد صوت يبدأ بـ`ar` ضمن الأصوات المتاحة فعليًا على الجهاز (`voices` بعد انتظار `voiceschanged`)، يتوقف الكود ويعرض رسالة عجز صريحة ("لا يوجد صوت عربي على هذا الجهاز… لن نستخدم صوتًا إنجليزيًا لتجنّب نطق مضلِّل") بدل استدعاء `speak()` بصوت غير مضمون.
- حالة ظاهرة جديدة: الصفحة الحالية، رقم المقطع الحالي/الإجمالي، تشغيل/إيقاف — معروضة في شريط أدوات القارئ.
- **محرك TTS مجاني بديل (WASM):** بحثت الخيار المطلوب (تضمين محرك عربي مفتوح المصدر يعمل كليًا في المتصفح، مثل Piper عبر WebAssembly). لم أضفه في هذا التسليم لسبب أذكره بصراحة بدل الادعاء بإنجازه: أي محرك من هذا النوع يحتاج ملفات نموذج صوتي (٥٠–٢٠٠+ ميجابايت للصوت العربي حسب الجودة)، ترخيصًا يُراجَع بندًا بندًا قبل التضمين، ورفع تحميل عند الطلب فقط (Lazy) يحتاج بنية بناء (Vite worker/asset) لم تكن موجودة في المشروع. تنفيذه في هذه الجلسة دون التحقق الفعلي من الترخيص والحجم والجودة كان سيعني الادعاء بشيء لم أختبره — وهذا يخالف صراحةً ما طلبته. الخيار المتاح الآن هو صوت الجهاز (`speechSynthesis`) فقط، مع رفض صريح وواضح حين لا يوجد صوت عربي، بدل نطق خاطئ. أوصي بهذا كبند منفصل قابل للتنفيذ لاحقًا (انظر القسم الأخير).

---

## ملخص الفجوة الهيكلية العامة

كل الأعطال الأربعة تنبع من سبب واحد مشترك: V0.6.x أضافت طبقة Supabase حقيقية (تخزين، RLS، جلسة مجهولة) **بمعزل عن** طبقة القارئ وطبقة الصوت، اللتين بقيتا مصممتين أصلًا لملف محلي مؤقت فقط (من نسخ سابقة أقدم). لم يكن هناك يوم واحد فيه "مصدر واحد للحقيقة" لموقع الكتاب. V0.7 توحّد هذا عبر تمرير هوية الكتاب ورابط موقَّع صراحة من المكتبة إلى القارئ، بدل تركهما نظامين منفصلين.

---

## هـ. التجربة المحلية المجانية (§5)

**الحالة قبل V0.7:** لا وجود إطلاقًا لأي مسار تحليل بنيوي محلي؛ كل "تحليل" في الكود القديم كان يمر حصرًا عبر `invokeBookAI` → Edge Function → OpenAI. لا يوجد بديل مجاني حقيقي لفهم بنية الكتاب.

**التنفيذ:**
- `src/lib/textAnalysis.ts`: وحدة نقية بلا أي اعتماد على المتصفح أو Supabase (باستثناء `crypto.subtle` في `hashFile`)، لذلك يمكن اختبارها بـ Node مباشرة بلا محاكاة متصفح. تُصدّر: `detectLanguage` (نسبة أحرف عربية/لاتينية)، `tokenize`، `topTerms` (بعد استبعاد قوائم كلمات وقف عربية/إنجليزية موثّقة داخل الملف نفسه)، `findHeadingCandidates` (قواعد ثابتة: طول السطر، ترقيم، علامات ترقيم ختامية)، و`buildLocalStructuralAnalysis` التي تجمع كل ما سبق في حمولة واحدة تحمل `engine:"local_js"` وحقل `disclosure` بالعربية ينص صراحة أن هذا **ليس** تلخيصًا ولا ترجمة ولا تحليلًا بالذكاء الاصطناعي.
- `src/lib/localAnalysis.ts`: ينزّل بايتات الكتاب من `spl-books` (نفس دلو التخزين الخاص بالمستخدم، RLS ساري)، يشغّل PDF.js لاستخراج نص كل صفحة (مع تقرير تقدم `{page,totalPages}`)، يحاول قراءة PDF metadata (يتجاهل الفشل بأمان)، ثم يبني التحليل ويحفظه عبر `saveLocalAnalysis`.
- **لم يُرسل نص الكتاب لأي خدمة خارجية في أي خطوة من هذا المسار** — التنزيل من مساحة تخزين المستخدم نفسه عبر نفس جلسة Supabase، والمعالجة بالكامل في JavaScript محلي.
- الواجهة في `PilotWorkspace` تعرض: عدد الصفحات/الكلمات، اللغة المكتشفة، عناوين مرشّحة (بحد أقصى للعرض)، أكثر الكلمات تكرارًا، ونص الإفصاح كاملًا — مع زر "إعادة التشغيل".
- **الاختبار الفعلي:** شغّلت 19 اختبارًا بـ`node --experimental-strip-types scripts/test-text-analysis.mjs` تغطي `detectLanguage`/`tokenize`/`topTerms`/`findHeadingCandidates`/`buildLocalStructuralAnalysis`/`validateManualImport` — **19/19 ناجحة**. هذا يغطي المنطق الحسابي فقط، وليس تكامل PDF.js/Supabase الفعلي (انظر التحفظ في TEST-RESULTS.md).

---

## و. تجربة التلخيص/الترجمة بلا تكلفة عبر استيراد JSON يدوي (§6)

**التنفيذ:**
- `validateManualImport` في `textAnalysis.ts` تتحقق من قالب موثّق بالكامل في `docs/manual-import-schema.md`، وتُجمّع كل الأخطاء دفعة واحدة (وليس أول خطأ فقط) — مثال: نص جذر ليس كائنًا، `overview.summary` مفقود، `source` خارج القيم الثلاث المسموحة، عنصر في `chapters[]` بلا `title`/`summary`.
- `saveManualImport` في `library.ts` تحفظ الحمولة بأكملها في `spl_analyses` بـ`kind:"manual_import"` و`source` تُطابق حرفيًا ما اختاره المستخدم (`manual_chatgpt`/`manual_claude`/`manual_other`)، مع `template_version` و`created_at`.
- الواجهة تعرض تسمية صريحة "مستورد يدويًا — غير منتَج عبر API" فوق أي محتوى مستورد، ولا تُخفي هذا التمييز أبدًا عن نتائج OpenAI الحقيقية.
- **لم يُرسل نص الكتاب أو أي جزء منه تلقائيًا لأي مكان** في هذا المسار — المستخدم هو من ينسخ/يلصق يدويًا خارج التطبيق تمامًا؛ التطبيق فقط يتحقق من الشكل ويحفظ.

---

## ز. عناصر واجهة معطّلة تبدو تفاعلية (§7)

جردت الأربعة عناصر التي ذكرتها ووجدتها فعلًا بلا أي وظيفة حقيقية في V0.6.3:

| العنصر | الحالة في V0.6.3 | الإصلاح |
|---|---|---|
| مربع البحث | `<input placeholder=…>` بلا `value`/`onChange`/أي منطق تصفية | ربط حقيقي بحالة `searchQuery`، وتصفية فعلية لكتب المكتبة المحفوظة والنماذج التجريبية معًا داخل `Library`، مع الانتقال التلقائي إلى صفحة المكتبة عند الإرسال |
| جرس الإشعارات | زر ثابت بشارة "٢" غير حقيقية (رقم مكتوب يدويًا في JSX) | تعطيل الزر (`disabled`) وإزالة الشارة المزيّفة، مع `title` صريح "قريبًا" |
| قائمة الملف الشخصي (⋮) | زر بلا أي `onClick` | تعطيل الزر مع `title` صريح "إعدادات الحساب — قريبًا" |
| نجوم التقييم في سجل التجربة | أزرار `[1..5]` بلا حالة، بلا حفظ فعلي — كانت `setSaved(true)` فقط تُظهر رسالة نجاح وهمية دون أي كتابة لقاعدة البيانات | ربط بحالة `rating`/`feature`/`note` فعلية، واستدعاء `saveFeedback()` الجديدة في `library.ts` التي تكتب فعليًا إلى جدول `spl_feedback` الموجود أصلًا في المخطط ولم يكن أي كود يكتب إليه من قبل |

لا يوجد الآن أي زر في هذا التسليم يبدو تفاعليًا بينما لا يفعل شيئًا: كل عنصر إما مربوط بمنطق حقيقي، أو معطّل بصريًا (`disabled` + `title` "قريبًا"/"coming soon").

---

## ح. تحصين الأمان والتكلفة (§8)

- **لا اعتماد على تأكيد العميل وحده كحماية حقيقية:** كل استدعاءات `spl_books`/`spl_analyses`/`spl_legal_consents`/`spl_reading_progress`/`spl_feedback` تمر عبر Supabase-js وتُفرض عليها سياسات RLS من `20260825_0001_spl_v05_pilot.sql` (`user_id = auth.uid()`) — هذا موجود أصلًا في المخطط ولم يتغيّر في V0.7؛ لم أُضعفه ولم أُنشئ أي مسار جديد يتجاوزه. لم أتحقق حيًا من أن هذه السياسات مُفعَّلة فعليًا على مشروعك (يتطلب اتصالًا لا تملكه هذه البيئة) — هذا موثّق كبند غير قابل للاختبار من هنا في `TEST-RESULTS.md`.
- **`spl-ai` غير قابل للوصول عمليًا في وضع التكلفة الصفرية:** أضفت `src/lib/config.ts` (`ZERO_COST_MODE = true`) وأضفت `if (ZERO_COST_MODE) return;` كأول سطر فعلي في كل من `process`/`ask`/`audio` داخل `PilotWorkspace` — الاستدعاءات الثلاثة لـ`invokeBookAI` تبقى موجودة في الكود (لإعادة التفعيل لاحقًا بسهولة) لكنها كود ميت في هذا البناء. `scripts/verify-zero-cost.mjs` يتحقق ثابتًا من هذا (عدد الحراسات ≥ عدد نقاط الاستدعاء) وشغّلته فعليًا: **كل الفحوصات نجحت (ALL CHECKS PASSED)**. هذا فحص بنيوي على النص المصدري وليس بديلًا عن مراقبة DevTools→Network حية أثناء استخدام فعلي — والسكربت نفسه يطبع هذا التحفظ صراحة في مخرجاته.
- **رقم "١٠ دولارات" لم يعد يُعرض كرصيد حي:** كان أصلًا نصًا وصفيًا في V0.6.3 ("الميزانية التي خصصتها عند بدء التجربة") وليس قراءة API لرصيد حقيقي؛ أبقيته كما هو ولم أُضِف أي استدعاء إلى Billing API (وهذا كان سيتطلب مفتاحًا سريًا إضافيًا، مخالف للقيد رقم ٢). أضفت فقط ملاحظة قفل صريحة توضح أن كل هذا القسم مُقفل بالكامل في بناء التكلفة الصفرية.
- **الإفصاح عن Anonymous Auth:** اللافتة الموجودة أصلًا في `Library` ("تنبيه النسخة التجريبية: دخولك مرتبط بهذا المتصفح حاليًا…") كانت موجودة في V0.6.3 وأبقيتها كما هي دون تعديل — تفصح بوضوح عن قيد ربط الجلسة بالمتصفح. لم أنفّذ حسابًا دائمًا (خارج نطاق هذا التسليم بطلبك)، لكنه مذكور كعمل مستقبلي مقترح أدناه.

---

## بند مستقبلي — محرك TTS عربي مجاني مضمّن (WASM)

هذا خيار **لم يُنفَّذ** في هذا التسليم (انظر السبب في قسم "د" أعلاه)، لكنه موثّق هنا كاقتراح عمل مستقبلي واضح الحدود:
- **الخيار الأقرب:** Piper TTS عبر WebAssembly — مفتوح المصدر بالكامل، يعمل داخل المتصفح بلا أي طلب شبكة بعد تحميل النموذج مرة واحدة، ويملك نماذج عربية جاهزة.
- **التكلفة الحقيقية:** حجم نموذج صوتي عربي واحد يتراوح تقريبًا بين ٥٠ و٢٠٠+ ميجابايت حسب الجودة المطلوبة — يجب أن يُحمَّل بشكل كسول (lazy، عند أول طلب استخدام فقط) وليس ضمن حزمة التطبيق الأساسية.
- **قبل أي تنفيذ فعلي يجب:** مراجعة ترخيص كل نموذج صوتي بندًا بندًا (تراخيص النماذج الصوتية المفتوحة تختلف عن ترخيص الكود نفسه)، وقياس زمن التحميل الفعلي على اتصال بطيء، وإضافة بنية Vite لتحميل ملفات WASM/النموذج ككائنات ثابتة منفصلة (asset) بدل تضمينها في الحزمة الرئيسية.
- لم يُدرَج هذا في `package.json` ولا في الكود لتفادي الادعاء بشيء لم أختبره فعليًا.
