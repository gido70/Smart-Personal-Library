# حالة المشروع

## V0.10.5-candidate — الصوت المكتمل وأساس نسخة المشرف

- الفرع: `feat/v1.9.0-reviewer-audio-state`، غير مدمج وغير منشور.
- الصوت: عند اكتمال جميع الأجزاء المتوقعة تتحول الواجهة إلى تشغيل فقط؛ تختفي إعادة الإنشاء واختيار الصوت والعينات والتكلفة والشراء والاستكمال.
- المشرفون: أضيف تصور تنفيذي لدور `reviewer` بصلاحية قراءة كتب مختارة وتسجيل الملاحظات، دون رفع أو تعديل أو حذف أو عمليات مدفوعة.
- الأمان: لم يُضف رابط تجريبي يتجاوز الصلاحيات؛ تنفيذ المشرف التالي يتطلب جداول مشاركة وسياسات RLS قبل إتاحة أي دعوة.
- التحقق: جميع الاختبارات وبناء الإنتاج ناجحة محليًا.
- التالي: رفع الفرع وإنشاء معاينة للصوت، ثم تنفيذ Migration دور المشرف وسياسات العزل في فرع مستقل بعد مراجعتها.

## الحالة الحالية — فرع مراجعة غير منشور

- الفرع: `feat/v1.8.3-knowledge-progress` مبني على `origin/main` عند `8055d90`.
- المنفذ: رفّان للكتب الأصلية والنسخ المعرفية، إظهار حالة النسخة المؤرشفة، ومؤشر دائم للمهام المدفوعة مع منع التكرار والاستئناف بالقراءة فقط، واستكمال أجزاء الصوت الناقصة دون إعادة الأجزاء المحفوظة.
- جودة الصوت: ستة أصوات قابلة للاختبار، وعينة إلزامية قبل الشراء الجديد، ونموذج صوت مثبت بإصدار محسن، وتعليمات راوٍ واحد ثابت، وربط اختيار الصوت بالمهمة حتى بعد تحديث الصفحة.
- تقدم الصوت: العدد المتوقع ظاهر قبل التأكيد، وأثناء الإنشاء تظهر حالة كل جزء منفردًا وعلامة ✓ فور حفظه.
- الأرشفة: تحتفظ بالسجل والغلاف والفهرسة والتصنيف والنتائج والصوت والأسئلة، وتحذف PDF الأصلي بعد حفظ حالة الأرشيف.
- الاستعادة: إعادة رفع الملف نفسه تستعيد السجل ببصمة SHA-256 دون تكرار النتائج؛ الاستعادة بلا ملف غير ممكنة بعد إزالة الأصل.
- التحقق: بناء الإنتاج واختبارات تنظيم المكتبة ناجحة. يلزم اختبار حي بحساب المالك وكتاب فعلي قبل النشر.
- النشر: لم يُنشر هذا الفرع ولم يُدمج بعد.

- الإصدار الجاري: V0.7.3-candidate (تجربة وظيفية بلا تكلفة OpenAI) — اعتمدها مالك المشروع للنشر بعد المراجعة المضادة الخفيفة.
- حالة الواجهة: V0.7.3-candidate هي النسخة المرسلة إلى GitHub Pages للاختبار الفعلي على iPhone والكمبيوتر؛ راجع `CLAUDE-AUDIT-REPORT.md` و`TEST-RESULTS.md` لمعرفة ما ثبت وما بقي BLOCKED.
- قاعدة البيانات: Migration معزولة باسم `spl_` نُفذت بنجاح داخل مشروع الفلاح، مع تفعيل الدخول المجهول وسياسات RLS
- التخزين: bucket خاص للكتب وآخر للصوت أُنشئا داخل مشروع الفلاح
- وظيفة `spl-ai`: مقفلة افتراضيًا من الخادم، ولا تُفتح مستقبلًا إلا بسر `SPL_PAID_AI_ENABLED=true`
- التحليل والأسئلة: وظيفة Supabase Edge Function جاهزة وتستخدم OpenAI Responses API
- الصوت: خلاصة مجزأة باستخدام `gpt-4o-mini-tts` مع تنبيه أنها مولدة بالذكاء الاصطناعي
- المسار المجاني: حفظ PDF وفتحه من المكتبة والتحليل البنيوي المحلي وصوت الجهاز لا تستدعي OpenAI
- المسار المدفوع: التحليل والترجمة والأسئلة والصوت الاحترافي مفصولة بأزرار واضحة وتأكيد ثانٍ مع تقدير نطاق التكلفة
- الإصدار السابق: V0.7.2؛ والإصدار الجاري للاختبار: V0.7.3-candidate.

## ترتيب التجارب

1. نشر الواجهة وفتحها بلا تسجيل ChatGPT. ✓
2. اختبار سلامة عرض PDF العربي في الوضع المطابق للأصل. ✓
3. مراجعة Migration V0.5 والموافقة الصريحة على تنفيذ SQL.
4. تفعيل Anonymous Sign-ins للتجربة وإضافة أسرار GitHub Pages العامة.
5. نشر وظيفة `spl-ai` وإضافة `OPENAI_API_KEY` كسر داخل Supabase.
6. اختبار كتاب عربي وكتاب إنجليزي وتسجيل النتائج.
