# حالة المشروع

- الإصدار الجاري: **V0.11.0 Book Journey Pilot** على الفرع `docs/wajeez-reference-and-v011-prep`.
- `main` لم يتغير بعد؛ النسخة الجديدة قيد البناء والمراجعة فقط.
- الهدف: تأسيس قاعدة قابلة لتوسع المكتبة من كتاب واحد إلى عشرات/مئات الكتب دون إعادة بناء تجربة المستخدم.
- الواجهة: هوية المنصة الحالية محفوظة، مع تحسين تنظيم المكتبة ومسار كل كتاب بالاستفادة من أنماط UX عربية مثل Wajeez دون نسخ هويته أو محتواه.
- القراءة: القارئ الحالي يعيد استخدام `spl_reading_progress` في Supabase لحفظ الصفحة والإشارات واستعادتها عبر الحساب.
- AI المدفوع: لا يعمل تلقائيًا. وظيفة `spl-ai` تبقى الحاجز المالي من الخادم وتعيد استخدام التحليل/الصوت المحفوظ بدل تكرار التكلفة.
- الصوت: المسار الاحترافي الحالي يستخدم `gpt-4o-mini-tts` للملخص فقط. الاعتماد النهائي مشروط باختبار نطق عربي فعلي على أجهزة المستخدم.
- التنبيهات V0.11: تمت إضافة بنية reminders + Web Push (Migration + Service Worker + dispatcher). لا تعتبر Background Push مفعلة فعليًا حتى تطبيق Migration وإضافة VAPID secrets واختبارها على جهاز حقيقي.
- المرجع العربي: Wajeez مسجل داخل وثائق المشروع كمرجع تجربة استخدام فقط.

## بوابة القبول قبل أي دفع OpenAI

1. `npm test` ناجح.
2. `npm run build` ناجح.
3. مراجعة مضادة للتغييرات وعدم وجود أسرار في الواجهة.
4. معاينة GitHub/Vercel على الهاتف والكمبيوتر.
5. الكتاب الموجود يفتح دون إعادة رفع.
6. حفظ موضع القراءة يعمل عبر الحساب.
7. مسار الكتاب يعكس الحالات الحقيقية: لم يبدأ / جاري / مكتمل.
8. اختبار التنبيه المحلي، ثم Web Push بعد تطبيق البنية المطلوبة.
9. اختبار الصوت العربي المدفوع على نص قبول قبل اعتماده.
10. بعد نجاح ما سبق فقط: فتح `SPL_PAID_AI_ENABLED=true` لتجربة كتاب واحد.
