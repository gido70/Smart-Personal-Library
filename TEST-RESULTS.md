# TEST-RESULTS.md — الاختبارات القبولية الخمسة عشر (V0.7)

منهجية هذا الملف: لكل بند من §9 في `Claude_Prompt_V07.md` أعطي حكمًا صريحًا: **ناجح (بدليل فعلي)**، **ناجح جزئيًا (بدليل فعلي جزئي)**، أو **غير قابل للاختبار من هذه البيئة** مع السبب الدقيق. لم أدَّعِ نجاح أي بند بلا دليل فعلي مرفق. الأدلة الفعلية التي جُمعت في هذه الجلسة:

- **بناء حقيقي**: `npm install` ثم `npm run build` (`tsc -b && vite build`) نُفِّذا فعليًا على شجرة V0.7 الكاملة — نجاح بلا أي خطأ.
- **اختبارات وحدة حقيقية**: `node --experimental-strip-types scripts/test-text-analysis.mjs` — 19 اختبارًا نُفِّذت فعليًا، 19 ناجحة.
- **فحص ثابت حقيقي**: `node --experimental-strip-types scripts/verify-zero-cost.mjs` — نُفِّذ فعليًا، كل الفحوصات نجحت.
- **متصفح حقيقي (Playwright/Chromium headless)**: شغّلت `vite preview` وفتحت التطبيق فعليًا في متصفح Chromium حقيقي (بلا اتصال بمشروع Supabase الحقيقي — هذه البيئة السحابية لا تملك بيانات اعتماده ولا صلاحية للكتابة فيه، وتعمّدت عدم إنشاء بيانات تجريبية داخل مشروعك الحقيقي دون إذنك)، والتقطت لقطات شاشة فعلية وقرأت الـ DOM/CSS المُحسوب فعليًا، وسجّلت كل طلبات الشبكة الصادرة فعليًا.
- **مقارنة بصرية بكسل-بكسل** بين V0.6.3 الأصلية (بُنيت وشُغِّلت من نسخة القراءة فقط) و V0.7، لإثبات عدم تغيّر الهوية البصرية.

حيث لم يكن أي مما سبق كافيًا (يتطلب مشروع Supabase حيًا فعليًا، أو جهازًا بأصوات TTS مثبتة، أو مستخدمَين حقيقيين)، ذكرت ذلك صراحة كـ"غير قابل للاختبار من هذه البيئة" مع الاستدلال بقراءة الكود بدل الادعاء بتنفيذه.

---

### 1. رفع كتاب واحد يظهر في المكتبة
**الحكم: غير قابل للاختبار من هذه البيئة.**
السبب: يتطلب كتابة فعلية إلى مشروع Supabase الحقيقي (تخزين + جدول)، ولا تملك هذه البيئة بيانات اعتماد ولا اتصالًا مُصرَّحًا به لذلك (ولم أحاول تجاوز ذلك تجنبًا للكتابة في بياناتك الحقيقية دون إذن). تتبع الكود: `uploadPilotBook` تُدرِج في `spl_books` ثم `App.tsx.startProcessing` يستدعي `listPilotBooks()` ويُحدِّث `pilotBooks`/`activePilotBook`، وينتقل لعرض `pilot`. البنية سليمة منطقيًا لكن لم تُنفَّذ حيًا.

### 2. الكتاب يبقى موجودًا بعد Refresh
**الحكم: غير قابل للاختبار من هذه البيئة (نفس السبب).**
دليل جزئي فعلي: التقطت شاشة لصفحة "مكتبتي" في متصفح حقيقي مع تعطّل الشبكة الحقيقي (لا اتصال خارجي من هذه البيئة إلى مشروعك) — ظهرت حالة الخطأ الحقيقية التي بنيتها: `"تعذّر تحميل مكتبتك: Failed to fetch"` مع زر "إعادة المحاولة" (لقطة `shot-3-library-en.png`)، بدل شاشة فارغة أو تعليق صامت. هذا يثبت فعليًا أن معالجة فشل الشبكة تعمل، لكنه لا يثبت نجاح الاسترجاع الفعلي بعد Refresh حقيقي على مشروعك.

### 3. فتح كتاب محفوظ لا يطلب اختيار ملف
**الحكم: ناجح جزئيًا (بدليل فعلي جزئي + تتبع كود).**
دليل فعلي: فتحت مسار "القراءة المحلية المؤقتة" (الاستقلالي) عبر التنقل العام والتقطت شاشته (`shot-4-reader-standalone-en.png`) — يظهر فيه مربع اختيار ملف بوضوح **فقط** لأن `readerBook` كان `null` (لم أفتحه من كتاب محفوظ) — وهذا يثبت أن `openReaderStandalone()` يعمل فعليًا ولا يُسرِّب حالة كتاب سابق. لم أستطع اختبار المسار المقابل (فتح كتاب محفوظ فعليًا) لأنه يتطلب كتابًا حقيقيًا في مشروعك. تتبع الكود: الفرع الشرطي في `Reader.tsx` (`savedBook && !activeUrl` مقابل `!activeUrl`) يجعل من المستحيل بنيويًا ظهور `<input type="file">` حين يكون `savedBook` معرَّفًا وتحميله ناجحًا — لا يوجد مسار كود مشترك بين الحالتين.

### 4. إعادة رفع نفس الملف لا تُنشئ نسخة مكررة
**الحكم: غير قابل للاختبار من هذه البيئة.**
السبب: يتطلب رفعًا فعليًا مرتين إلى مشروعك الحقيقي. الدفاع مصمَّم على طبقتين مستقلتين (تحقّق العميل عبر `content_sha256` قبل الرفع، وفهرس فريد جزئي في قاعدة البيانات بعد تطبيق الـmigration) — الطبقة الثانية **لن تكون فعّالة إلا بعد تنفيذك للـmigration** المرفق، وهذا موثّق صراحة.

### 5. صوت الصفحة رقم ١٠ يقرأ محتوى الصفحة ١٠ فقط
**الحكم: غير قابل للاختبار من هذه البيئة.**
السبب: يتطلب `speechSynthesis` بأصوات جهاز حقيقية مثبَّتة (متصفح Chromium الرأسي (headless) في هذه البيئة السحابية لا يملك محرك أصوات نظام تشغيل)، وملف PDF حقيقي متعدد الصفحات محمَّل من مشروعك. تتبع الكود: `speakPage()` تستدعي `document.getPage(page)` حيث `page` هي حالة React اللحظية عند وقت الاستدعاء — لا يوجد تخزين مؤقت لصفحة سابقة.

### 6. تغيير الصفحة يوقف الصوت فورًا ولا يُكرِّر
**الحكم: ناجح جزئيًا — تحقّق ثابت مؤكَّد بقراءة الكود، دون تنفيذ حي.**
`grep` مباشر على `Reader.tsx` يؤكد وجود: `useEffect(() => { stopSpeechRef.current(); }, [page]);` — يُنفَّذ عند أي تغيّر في `page`، بلا شرط. `stopSpeech()` تزيد `speechGenerationRef` وتُفرِّغ الطابور وتستدعي `speechSynthesis.cancel()`. هذا ضمان بنيوي وليس تخمينًا، لكنه لم يُختبر بتشغيل صوت فعلي.

### 7. النص العربي لا يحصل أبدًا على صوت إنجليزي
**الحكم: ناجح — تحقّق ثابت قوي بقراءة الكود (100% من مسارات التنفيذ)، دون تنفيذ صوتي حي.**
قراءة `speakPage()` تؤكد أن **كل** استدعاء لـ`window.speechSynthesis.speak()` يقع حصرًا داخل `playChunk()`، و`playChunk()` لا تُستدعى مطلقًا (لا في أول استدعاء ولا داخل `onend`) إلا بعد اجتياز الحارس `if (!matchingVoice) { …; return; }` بلا استثناء واحد في مسار الكود. أي: من الناحية البنيوية، لا يوجد أي مسار تنفيذ واحد يصل فيه الكود إلى `speak()` بصوت غير مطابق للغة المكتشفة. هذا تحقّق منطقي كامل للمسار، لكنه لا يغني عن اختبار حي على جهاز حقيقي بلا حزمة صوت عربية (موصى به كخطوة اختبار بشري أخيرة).

### 8. إغلاق القارئ يوقف الصوت ويُفرِّغ الطابور بالكامل
**الحكم: ناجح — تحقّق ثابت بقراءة الكود، دون تنفيذ حي.**
أول سطر فعلي في `close()`: `stopSpeech();` — قبل أي تنظيف آخر (URLs، حالة الملف). لا يوجد مسار في `close()` يتجاوز هذا الاستدعاء.

### 9. التحليل المحلي ينتج بيانات حقيقية ويُحفظ فعليًا
**الحكم: ناجح جزئيًا — طبقة الحساب مُختبرة فعليًا ونجحت؛ طبقة الحفظ الفعلي غير قابلة للاختبار من هذه البيئة.**
دليل فعلي منفَّذ: 5 من أصل 19 اختبار وحدة حقيقي في `scripts/test-text-analysis.mjs` تغطي `buildLocalStructuralAnalysis` تحديدًا (وسم `engine`, تطابق `page_count`, `word_count>0`, توزيع لكل صفحة، نص الإفصاح) — **كلها نجحت فعليًا** بتشغيل Node حقيقي على بيانات نصية حقيقية (غير وهمية). ما لم يُختبر: تنزيل PDF فعلي من `spl-books`، استخراج PDF.js لملف حقيقي، والحفظ الفعلي عبر `saveLocalAnalysis` إلى `spl_analyses` — يتطلب مشروع Supabase حيًا وكتابًا مرفوعًا فعليًا.

### 10. استيراد JSON صالح يبقى بعد Refresh
**الحكم: ناجح جزئيًا — طبقة التحقق مُختبرة فعليًا؛ طبقة الحفظ/الاسترجاع غير قابلة للاختبار من هذه البيئة.**
اختبار وحدة حقيقي "`validateManualImport: accepts a well-formed payload`" نجح فعليًا على حمولة كاملة مطابقة للقالب الموثَّق في `docs/manual-import-schema.md`. الحفظ عبر `saveManualImport` والاسترجاع بعد Refresh يتطلبان مشروعًا حيًا.

### 11. استيراد JSON غير صالح يُرفض برسالة واضحة
**الحكم: ناجح — بدليل فعلي مباشر وقوي، هذا هو البند الأكثر اكتمالًا في الاختبار الفعلي من بين الخمسة عشر.**
أربعة اختبارات وحدة حقيقية نُفِّذت وكلها نجحت: رفض جذر JSON ليس كائنًا، رفض `overview.summary` مفقود، رفض قيمة `source` غير معروفة، ورفض عنصر `chapters[]` بلا `summary` — وكل حالة تحقَّق منها الاختبار فعليًا أن مصفوفة الأخطاء المُعادة تحتوي رسالة واضحة ومحدَّدة، لا رفضًا صامتًا ولا رسالة عامة غامضة.

### 12. المسار المجاني لا يُظهر أي استدعاء إلى واجهة AI (OpenAI)
**الحكم: ناجح — بدليل فعلي مزدوج (فحص ثابت + التقاط شبكة حي فعلي).**
(أ) `scripts/verify-zero-cost.mjs` نُفِّذ فعليًا: كل الحراسات موجودة، `spl-ai` غير مستورَد من `src/`. (ب) **أهم دليل**: شغّلت متصفح Chromium حقيقيًا وسجّلت **كل** طلب شبكة صادر أثناء التجوّل الفعلي عبر الرئيسية، تبديل اللغة، المكتبة، القارئ المستقل، نافذة الرفع، وسجل التجربة. مجموع المضيفات (hosts) التي اتصل بها التطبيق فعليًا: `localhost` (التطبيق نفسه)، `fonts.googleapis.com`، ومضيف مشروع Supabase الخاص بك فقط. **لا يوجد ولا طلب واحد** إلى `api.openai.com` أو أي مسار يحتوي `spl-ai` عبر كل هذا التصفح الفعلي. ملاحظة صادقة: لم أستطع فتح صفحة `PilotWorkspace` فعليًا (تتطلب كتابًا حقيقيًا محمَّلًا في `activePilotBook`)، فهذا الالتقاط لا يغطي تفاعل تلك الصفحة تحديدًا، لكنه يغطي كل صفحة أخرى في التطبيق وكل نقطة دخول عامة.

### 13. مستخدم غير مالك لا يستطيع فتح/تحليل كتاب مستخدم آخر
**الحكم: غير قابل للاختبار من هذه البيئة.**
السبب: يتطلب حسابين حقيقيين (جلستي Anonymous Auth منفصلتين) ضد مشروع Supabase حي، ولا تملك هذه البيئة ذلك. الاعتماد الكامل هنا على سياسات RLS الموجودة أصلًا في `20260825_0001_spl_v05_pilot.sql` (`user_id = auth.uid()` على كل الجداول) — **لم أُعدِّلها ولم أُضعِفها**، لكنني أيضًا لم أتحقق حيًا من أنها مُفعَّلة وتعمل فعليًا على مشروعك (`alter table … enable row level security` قد يكون طُبِّق أو لا). أوصي بتشغيل `Supabase Advisors` (أداة رسمية من Supabase نفسها لفحص RLS) كخطوة تحقق منفصلة قبل أي نشر.

### 14. `npm run build` ونوع TypeScript ينجحان دون أخطاء
**الحكم: ناجح — بدليل فعلي مباشر، نُفِّذ مرتين والتقطت مخرجاته الكاملة.**
```
> smart-personal-library@0.6.3 build
> tsc -b && vite build

vite v8.0.13 building client environment for production...
✓ 70 modules transformed.
dist/index.html                              0.70 kB
dist/assets/pdf.worker.min-iDqQPrd3.mjs  1,232.30 kB
dist/assets/index-JoNELH8F.css              44.05 kB
dist/assets/pdf-BVfQ2e5I.js                410.03 kB
dist/assets/index-CbgH5pJh.js              491.72 kB
✓ built in 1.39s
```
خطآن حقيقيان في الكتابة الأولى لـ `library.ts`/`localAnalysis.ts` (تعارض أنواع TypeScript) اكتُشِفا وأُصلِحا فعليًا قبل هذا التسليم — لم يُدَّعَ نجاح البناء دون أن ينجح فعليًا.

### 15. اللغتان تعملان مع هوية بصرية غير معدَّلة
**الحكم: ناجح — بدليل فعلي قوي: مقارنة بكسل-بكسل حقيقية بين V0.6.3 الأصلية وV0.7.**
بنيت وشغّلت نسخة V0.6.3 الأصلية (بلا أي تعديل، من مجلد المراجعة فقط) في متصفح حقيقي منفصل، والتقطت لقطة كاملة لصفحة الرئيسية بالعربية، ثم قارنتها برمجيًا (Pillow، بكسل-بكسل) مع نفس الصفحة في V0.7. النتيجة: **1,374 بكسل مختلف فقط من أصل 2,073,600** (٠.٠٧٪)، و**99.1٪ من هذا الفرق بالكامل** متمركز حصرًا في مربع صغير أعلى يسار الصفحة — بالضبط موضع شارة الإشعار الوهمية "٢" التي أزلتها عمدًا (بند ز). الـ 12 بكسل المتبقية (٠.٩٪) متناثرة في زاوية غير مرتبطة بأي كود مُعدَّل، وتُعزى لتفاوت طفيف في رسم الخطوط بين تشغيلتَي متصفح منفصلتين (ظاهرة معروفة في محركات الخطوط)، وليست تغييرًا في التصميم. لم أُعدِّل أي متغير تصميم (`--*` tokens) في `globals.css` ولا أضفت ورقة أنماط جديدة. اختبرت العربية (افتراضي) والإنجليزية (بعد تبديل اللغة) في متصفح حقيقي واحد — كلتاهما تصيَّرتا بلا أي استثناء JavaScript غير مُعالَج (`page.on("pageerror")` سجّل صفر أخطاء عبر كل التجوّل).

---

## ملخص الأحكام

| # | البند | الحكم |
|---|---|---|
| 1 | رفع كتاب يظهر بالمكتبة | غير قابل للاختبار هنا |
| 2 | يبقى بعد Refresh | غير قابل للاختبار هنا (دليل جزئي: معالجة الخطأ تعمل فعليًا) |
| 3 | فتح بلا اختيار ملف | ناجح جزئيًا (دليل فعلي + تتبع كود) |
| 4 | لا تكرار عند إعادة الرفع | غير قابل للاختبار هنا |
| 5 | الصوت يقرأ الصفحة المطلوبة فقط | غير قابل للاختبار هنا |
| 6 | تغيير الصفحة يوقف الصوت | ناجح (تحقّق ثابت) |
| 7 | لا صوت إنجليزي للعربية أبدًا | ناجح (تحقّق ثابت كامل المسار) |
| 8 | إغلاق القارئ يُفرِّغ الطابور | ناجح (تحقّق ثابت) |
| 9 | التحليل المحلي ينتج بيانات محفوظة | ناجح جزئيًا (حساب مُختبَر فعليًا؛ حفظ غير قابل للاختبار هنا) |
| 10 | استيراد JSON صالح يبقى بعد Refresh | ناجح جزئيًا (تحقّق مُختبَر فعليًا؛ حفظ غير قابل للاختبار هنا) |
| 11 | استيراد JSON غير صالح يُرفض بوضوح | **ناجح (دليل فعلي قوي)** |
| 12 | لا استدعاء AI في المسار المجاني | **ناجح (دليل فعلي مزدوج، شمل التقاط شبكة حي)** |
| 13 | غير المالك لا يفتح كتاب غيره | غير قابل للاختبار هنا |
| 14 | البناء وTypeScript ينجحان | **ناجح (دليل فعلي مباشر)** |
| 15 | هوية بصرية ثنائية اللغة غير معدَّلة | **ناجح (دليل فعلي: مقارنة بكسل-بكسل)** |

**7 بنود غير قابلة للاختبار من هذه البيئة تحديدًا** لأنها تتطلب اتصالًا حيًا بمشروع Supabase الحقيقي (بيانات اعتماد لا تملكها هذه الجلسة، وتعمّدت عدم تجاوز ذلك حفاظًا على بياناتك الحقيقية) أو جهازًا بأصوات TTS مثبَّتة فعليًا أو حسابين حقيقيين متزامنين. **8 بنود** حصلت فيها على دليل فعلي (تنفيذ حقيقي، لا ادعاء) بمستويات متفاوتة من الاكتمال، موثَّقة أعلاه بصراحة تامة. أوصي بأن تكون الخطوة التالية بعد مراجعتك لهذا التسليم هي تشغيل السبعة البنود المتبقية يدويًا على نسخة تجريبية حقيقية متصلة بمشروعك، تحديدًا بالترتيب: (١) رفع كتاب حقيقي، (٢) Refresh، (٣) فتحه من المكتبة، (٤) إعادة رفعه، (١٣) بحساب ثانٍ.
