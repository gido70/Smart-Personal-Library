# نتائج اختبارات القبول الخمسة عشر — V0.7.3-candidate

بيئة التنفيذ العامة: حاوية Linux سحابية معزولة، Node 22، Chromium فقط عبر Playwright (`/opt/pw-browsers` — لا يوجد محرك WebKit حقيقي ولا جهاز iPhone فعلي). **تحققتُ مباشرة أن هذه البيئة لا تملك اتصال شبكة بمشروع Supabase الفعلي** (`curl` إلى `nmbbahzzogspuuvpsxud.supabase.co` انتهى بلا استجابة/رفض اتصال) — وهذا يعني أن أي اختبار يحتاج جلسة Supabase حية (رفع، حفظ، قراءة، صوت جهاز حقيقي، RLS بحسابين) **BLOCKED** فعلًا وليس افتراضًا احترازيًا فقط. لم أحاول الالتفاف على هذا القيد بأي وسيلة (لا محاكاة تدّعي أنها اتصال حقيقي، ولا بيانات ملفّقة).

القاعدة المتبعة: `PASS` فقط عندما يوجد دليل تنفيذ فعلي حقيقي (أمر شُغِّل، لقطة شاشة، اختبار آلي ناجح). `BLOCKED` عندما تتطلب البيئة شيئًا غير متاح هنا (اتصال Supabase حي، جهاز iPhone، محرك WebKit). لا يوجد أي `PASS` في هذا الملف غير مدعوم بدليل مذكور صراحة.

---

### 1. رفع PDF واحد وحفظه
**BLOCKED.** يتطلب جلسة Supabase حية (`ensurePilotSession` → `signInAnonymously` → `storage.upload` → `insert`). لا اتصال شبكة بمشروعك من هذه البيئة. الكود نفسه (`uploadPilotBook` في `src/lib/library.ts`) لم يتغيّر في V0.7.3 ومطابق لما اجتاز مراجعة V0.7.1 السابقة.
البيئة: —

### 2. ظهوره بعد إعادة تحميل الصفحة
**BLOCKED.** نفس سبب البند 1 — يتطلب `listPilotBooks()` حيًا. لاحظ أن `Library` تعرض الآن رسالة صادقة («تعذر تحميل مكتبتك: Failed to fetch») مع زر إعادة محاولة بدل الانهيار الصامت عند تعذر الشبكة — هذا شوهد فعليًا أثناء فحص العرض على هاتف ضيق (البند 14 أدناه)، وهو دليل غير مباشر أن حالة الفشل تُعالَج بلطف، لا دليل على نجاح التحميل نفسه.
البيئة: Chromium (Playwright)، لقطة `mobile-library.png`.

### 3. فتحه بلا إعادة رفع
**BLOCKED.** يعتمد على البندين 1 و2 وعلى `createBookSignedUrl` الحي. لم يتغيّر الكود في V0.7.3.
البيئة: —

### 4. منع رفع النسخة نفسها ثانية
**BLOCKED** للتحقق الحي الكامل (رفع فعلي مرتين). **مراجعة الكود:** منطق الدفاع (`hashFile` + بحث `content_sha256` قبل الرفع في `uploadPilotBook`) سليم بنيويًا ولم يتغيّر. Migration الـ V0.7 نُفذت سابقًا بنجاح في مشروع Supabase الفعلي وفق نتيجة SQL التي قدّمها مالك المشروع؛ لذلك يبقى المطلوب هنا اختبار المنع حيًا بعد النشر، لا إعادة تنفيذ Migration. لوحة مراجعة التكرارات الجديدة (`DuplicateReviewPanel`) تتيح كشفًا وحذفًا يدويًا آمنًا للتكرارات القديمة.
البيئة: —

### 5. ظهوره كبطاقة غلاف حقيقية لا كسطر عام
**PASS (على مستوى المكوّن البصري، وليس عبر مكتبة حية متصلة بـ Supabase).**
بنيت صفحة معاينة مستقلة تستخدم *نفس ملف CSS المُصدَّر فعليًا من `npm run build`* (`dist/assets/index-*.css`) مع نفس البنية HTML التي يولّدها مكوّن `LiveBookCard` الجديد، بعنوان الكتاب نفسه من الدليل («كيف استخدم كلاود؟»). النتيجة: غلاف ملوَّن بعنوان واضح، وسم «كتابك»، ونص فرعي صادق («لم تُحدَّد بعد» بدل «UNKNOWN»)، وحالة صادقة («محفوظ — بانتظار التحليل المحلي») — بنفس الجودة البصرية للنماذج الثلاثة تمامًا، لا سطرًا مسطّحًا.
البيئة: Chromium (Playwright) 375×500، لقطة `card-preview.png` (مُرفَقة).

### 6. نجاح التحليل المحلي على Chromium
**PASS (لسلسلة استدعاء PDF.js نفسها التي يستخدمها التحليل المحلي)، BLOCKED للتدفق الكامل عبر التطبيق والمكتبة الحية.**
شغّلت *نفس* الاستدعاءات الحقيقية (`pdfjs.getDocument → doc.getPage(1) → page.getTextContent()`) من نفس نسخة `pdfjs-dist@5.7.284` المثبَّتة، على ملف PDF حقيقي (نص "Hello Smart Personal Library test page."، أُنشئ بـ reportlab)، داخل Chromium حقيقي: نجح الاستخراج الكامل. 32/32 اختبار وحدة لدوال `textAnalysis.ts`/التي يعتمد عليها هذا المسار ناجحة أيضًا (`npm run test:unit`).
لم أُشغّل التدفق الكامل (زر «شغّل التحليل المحلي المجاني» داخل التطبيق مع كتاب محفوظ فعليًا) لأن ذلك يحتاج تنزيل الملف من Supabase Storage (BLOCKED، القسم أعلاه).
البيئة: Chromium 141 (Playwright) عبر `/opt/pw-browsers/chromium-1194`، Node 22؛ ناتج: `SUCCESS:Hello Smart Personal Library test page.`.

### 7. نجاح التحليل المحلي على WebKit — لا ادّعاء iPhone فعليًا
**iPhone حقيقي: BLOCKED — لا يوجد جهاز فعلي ولا محرك WebKit في هذه البيئة (`/opt/pw-browsers` يحوي فقط Chromium).**
**دليل جزئي (محاكاة، مُعلَّم صراحة على أنه محاكاة لا اختبار حقيقي):** بنيت محاكاة Chromium مع حذف `Promise.withResolvers` من الصفحة قبل التحميل (`page.addInitScript`)، لتقليد محرك JS لا يدعم هذه الدالة (كحال Safari <17.4). النتائج الثلاث:

| السيناريو | النتيجة الفعلية |
|---|---|
| Chromium عادي (الدالة موجودة) | `SUCCESS:Hello Smart Personal Library test page.` |
| محاكاة محرك قديم، **بلا** إصلاح V0.7.3 | `ERROR:Promise.withResolvers is not a function` |
| محاكاة محرك قديم، **مع** إصلاح V0.7.3 (`src/lib/polyfills.ts`) | `SUCCESS:Hello Smart Personal Library test page.` |

كذلك تحققت أن حزمة الإنتاج الفعلية (`dist/`) تُركّب الإصلاح قبل أي كود آخر: تحميل `dist/index.html` بنفس المحاكاة أظهر أن `Promise.withResolvers` أصبحت موجودة فور تنفيذ الحزمة، بلا أي خطأ JS غير مُلتَقط.
هذا يثبت السبب الجذري ويثبت أن الإصلاح يعمل **تحت المحاكاة**، لكنه **لا يثبت** نجاحًا على iPhone فعلي — الفرق الوحيد المتبقي غير القابل للاختبار هنا هو سلوك WebKit الحقيقي (وليس مجرد غياب دالة JS واحدة) على جهاز فعلي.
البيئة: Chromium 141 (Playwright)، محاكاة `page.addInitScript`، لا جهاز iPhone.

### 8. قراءة صفحة عربية ذات طبقة نص بصوت عربي متاح
**BLOCKED.** يحتاج كتابًا محفوظًا فعليًا (Supabase، BLOCKED) وأصوات جهاز حقيقية (`speechSynthesis.getVoices()`، تختلف فعليًا بين المتصفح الحقيقي على جهاز والحاوية الآلية بلا واجهة صوت). منطق اختيار الصوت واستخراج النص في `Reader.tsx::speakPage` لم يتغيّر عن V0.7.1 (الذي اجتاز مراجعته)، باستثناء تسجيل الأخطاء الحقيقية بدل ابتلاعها بصمت — وهذا تغيير تشخيصي فقط.
البيئة: —

### 9. سلوك واضح لصفحة غلاف أو PDF مصوَّر بلا طبقة نص
**PASS جزئي:** الرسالة الصادقة («هذه الصفحة فارغة أو لا تحتوي نصًا كافيًا… تحتاج OCR لا نوفره في هذه النسخة المجانية») موجودة من V0.7.1 ولم تتغيّر. **الميزة الجديدة** (اقتراح أقرب صفحة نصية وزر «انتقل إليها»، دالة `findNextTextPage`) اجتازت فحص الأنواع الكامل (`tsc -b`) والبناء، لكن **لم تُختبر حيًّا** ضد PDF حقيقي بصفحة غلاف داخل التطبيق (يحتاج مصدرًا محفوظًا، BLOCKED). أُصنّفها **BLOCKED** للسلوك الحي رغم أن الكود منفَّذ ومُتحقَّق من أنواعه، حرصًا على معيار الصدق (لا PASS بلا دليل تنفيذ فعلي).
البيئة: `tsc -b` (فحص أنواع ناجح)، بدون تنفيذ حي.

### 10. تنقل الصفحات دون تكرار صوت الصفحة السابقة
**PASS (اختبار انحدار آلي، وليس تشغيل صوت حي على جهاز).**
الآلية المُصلِحة لهذه المشكلة (رمز جيل الطلب `speechGenerationRef` يُستهلَك *قبل* أي `await` لاستخراج PDF) من V0.7.1 ولم أُغيّرها؛ فقط أضفت إعادة تصفير `suggestedTextPage`/`scanningForText` عند تغيّر الصفحة حتى لا يبقى اقتراح قديم معلَّقًا. فحص انحدار مصدري آلي موجود أصلًا (`scripts/verify-v071-release.mjs`) ما زال ينجح بعد كل تعديلاتي: `PASS speech request is claimed before PDF awaits`.
البيئة: `node scripts/verify-v071-release.mjs` (Node 22) — فحص بنيوي على المصدر، وليس تسجيل صوت فعلي.

### 11. عمل البحث المحلي وإرجاع أرقام صفحات
**PASS — دليل آلي حقيقي كامل.**
5 اختبارات وحدة جديدة في `scripts/test-text-analysis.mjs` تبني تحليلاً محليًا حقيقيًا (`buildLocalStructuralAnalysis`) على نصوص متعددة الصفحات، ثم تستدعي `searchInsideBook` فعليًا وتتحقق: النتائج تشير لأرقام الصفحات الصحيحة (1 و2 لا 3)، والمقتطف يحتوي فعلاً كلمة البحث، والبحث بلا نتائج يُعيد مصفوفة فارغة لا خطأ، وغياب `pages_text` (كتاب قديم) يُعيد `[]` بأمان بدل رمي استثناء.
البيئة: `node --experimental-strip-types scripts/test-text-analysis.mjs` — 32/32 ناجح.

### 12. عدم وجود أي طلب شبكة إلى OpenAI/Anthropic/Google AI
**PASS (فحص بنيوي للمصدر، وليس التقاط DevTools Network حيًا فعليًا — والفحص نفسه يذكر هذا التحفظ).**
`scripts/verify-zero-cost.mjs` (لم يتغيّر) ما زال ينجح بالكامل: `ZERO_COST_MODE` صحيح، كل استدعاء لـ`invokeBookAI` محروس بشرط `ZERO_COST_MODE`، الدالة السحابية المدفوعة خارج حزمة `src/` ولا تُستورَد مباشرة، وتفشل مغلقة افتراضيًا (`SPL_PAID_AI_ENABLED !== "true"`). لم أُضف أي استدعاء شبكة جديد في V0.7.3 (كل الميزات الجديدة — البحث، التعبئة الرجعية، الترقية — تستخدم Supabase الحالي أو منطقًا محليًا بحتًا).
البيئة: `node --experimental-strip-types scripts/verify-zero-cost.mjs`.

### 13. بقاء RLS وعزل ملكية الكتب
**BLOCKED للتحقق الحي بحسابين حقيقيين.** **مراجعة كود مباشرة (دليل داعم، لا بديل عن اختبار حي):** سياسة RLS الأساسية (`for all ... using(user_id = auth.uid()) with check(user_id = auth.uid())`) لم تتغيّر؛ كل استعلام جديد في V0.7.3 (`backfillBookMetadataFromLocalAnalysis`, `groupDuplicateBooks` عبر `listPilotBooks` الموجودة، حذف عبر `rollbackPilotBook` الموجودة) يمرّ عبر نفس عميل Supabase المصادَق (`ensurePilotSession`)، ولا يوجد أي استعلام جديد يستخدم `service_role` أو يتجاوز RLS. تحديث `backfillBookMetadataFromLocalAnalysis` لا يُغيّر `user_id` في أي صف (يقتصر على `source_language`/`title`)، فلا يمكن أن يخالف شرط `with check`.
البيئة: مراجعة مصدر يدوية فقط.

### 14. عمل الواجهة على عرض هاتف ضيق دون عناصر مقطوعة
**PASS — دليل آلي حقيقي.**
حمّلت حزمة الإنتاج الفعلية (`dist/`) في Chromium بعرض 375px (فئة iPhone SE)، للصفحة الرئيسية وصفحة المكتبة (بما فيها حالتها الصادقة عند تعذر الشبكة): `document.documentElement.scrollWidth > clientWidth` = **false** في الحالتين (لا تمرير أفقي، أي لا قطع عناصر خارج حدود الشاشة). لقطتا شاشة مرفقتان (`mobile-home.png`, `mobile-library.png`) تُظهران تخطيطًا مطابقًا لصور الدليل الحقيقية من الهاتف، بلا عناصر مقطوعة ظاهريًا.
البيئة: Chromium 141 (Playwright)، viewport 375×667.

### 15. فصل الكتب النموذجية عن كتب المستخدم وعدم عرض تقدم مزيف
**PASS — مراجعة كود + دليل بصري.**
الكتب النموذجية (`books` الثابتة في `App.tsx`) منفصلة بنيويًا عن `pilotBooks` (المصدر Supabase) منذ V0.7 ولم يتغيّر هذا. بطاقة الكتاب الحقيقي الجديدة (`LiveBookCard`) **لا تحتوي أي شريط تقدّم (`Bar`) ولا أي رقم مئوية** — فقط وسم «كتابك» وحالة نصية صادقة (محفوظ/قيد المعالجة/جاهز/فشل)؛ بطاقة النموذج (`BookCard`) وحدها تعرض `Bar`/نسبة تقدّم، وهي بيانات ثابتة معروفة بأنها عرض تجريبي (`نموذج تجريبي`/`Demo Edition` في بيانات المؤلف). لقطة `card-preview.png` تُظهر هذا مباشرة: لا شريط تقدّم على بطاقات الكتب الحقيقية.
البيئة: مراجعة مصدر + Chromium (Playwright)، `card-preview.png`.

---

## ملخص الحالة

| # | الاختبار | النتيجة |
|---|---|---|
| 1 | رفع وحفظ | BLOCKED |
| 2 | ظهور بعد التحديث | BLOCKED |
| 3 | فتح بلا إعادة رفع | BLOCKED |
| 4 | منع تكرار الرفع | BLOCKED (مراجعة كود فقط) |
| 5 | بطاقة غلاف حقيقية | **PASS** |
| 6 | تحليل محلي — Chromium | **PASS** (المسار الجوهري)، BLOCKED (التدفق الكامل) |
| 7 | تحليل محلي — WebKit/iPhone | BLOCKED حقيقي؛ **دليل محاكاة PASS** |
| 8 | صوت عربي على صفحة عربية | BLOCKED |
| 9 | صفحة غلاف/مصوَّرة | BLOCKED (كود مُنفَّذ وغير مُختبَر حيًّا) |
| 10 | لا تكرار صوت عند التنقل | **PASS** (انحدار آلي) |
| 11 | بحث محلي بأرقام صفحات | **PASS** |
| 12 | لا طلبات لخدمات AI مدفوعة | **PASS** (بنيوي) |
| 13 | RLS وعزل الملكية | BLOCKED (مراجعة كود فقط) |
| 14 | واجهة هاتف ضيق | **PASS** |
| 15 | فصل النماذج عن كتب المستخدم | **PASS** |

**لا يوجد أي FAIL معروف في هذه النسخة** — كل ما لم يُختبر مُعلَّم `BLOCKED` بصراحة، لا `PASS` مُتفائل. هذا **ليس** «نسخة مكتملة» بمعيار `FAILURE-EVIDENCE.md`: النجاح الحقيقي على جهاز فعلي (البنود 1-4، 7-9، 13) لا يزال غير مُثبَت، ويحتاج إما اتصال هذه البيئة بمشروع Supabase الفعلي، أو تشغيل هذه الحزمة يدويًا على جهاز حقيقي واتصال حقيقي من طرفك.
