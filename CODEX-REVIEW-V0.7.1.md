# مراجعة وتنفيذ V0.7.1

## القرار

هذه النسخة هي خط الأساس المصحح للتجربة الوظيفية المجانية. لا تستدعي OpenAI من الواجهة، كما أن وظيفة `spl-ai` تفشل افتراضيًا من الخادم ما لم يُضف مالك المشروع صراحة السر `SPL_PAID_AI_ENABLED=true` مستقبلًا.

## الإصلاحات بعد مراجعة V0.7

- العودة من القارئ إلى صفحة الكتاب بدل شاشة اختيار ملف جديد.
- إلغاء طلب الصوت القديم قبل اكتمال استخراج الصفحة لمنع قراءة صفحة سابقة.
- تأجيل حفظ تقدم القراءة حتى استعادة الصفحة والعلامات من Supabase.
- عدم تجاهل فشل إقرار الحقوق عند اكتشاف كتاب مكرر.
- استرجاع حقلي مصدر التحليل وإصدار القالب بعد تحديث الصفحة.
- الحفاظ على فواصل أسطر PDF.js حتى يعمل اكتشاف العناوين المرشحة.
- قصر رفع النسخة التجريبية على PDF؛ دعم EPUB مؤجل حتى إضافة قارئ مناسب.
- اختيار صوت جهاز محدد من الأصوات التي يعرضها المتصفح.
- قفل Edge Function افتراضيًا لحماية الرصيد حتى من الاستدعاء المباشر.
- جعل Migration V0.7 ذرّية مع تحقق نهائي قبل `COMMIT`.
- تعطيل أزرار نماذج العرض التي لا ترتبط ببيانات حقيقية.

## التحقق المنفذ

- `npm run build`: ناجح.
- اختبارات التحليل والنص: 21/21 ناجحة.
- فحص وضع التكلفة الصفرية: ناجح.
- فحص متطلبات الإصدار V0.7.1: ناجح بالكامل.
- `git diff --check`: ناجح.

## خطوة التشغيل المطلوبة

يجب تنفيذ `supabase/migrations/20260826_0002_spl_v07_zero_cost.sql` مرة واحدة داخل مشروع Supabase قبل اختبار منع التكرار وحفظ التحليل المحلي والاستيراد اليدوي. لا تحتوي بيئة التنفيذ الحالية على اتصال إدارة مباشر بمشروع Supabase، لذلك لا يُعد الملف منفذًا لمجرد وجوده في GitHub.
