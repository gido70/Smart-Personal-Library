# إصلاح رفع الكتب وحماية الأرشيف — 2026-09-23

الفرع: `fix/reliable-book-upload`، مبني على `43e986a` (PR #36).
نسخة الواجهة الأساسية: V0.10.5؛ مراجعة التخزين المؤقت: `upload-3`.

## قاعدة المشروع المعتمدة

- ستة ملفات PDF أصلية نشطة كحد أقصى، كل ملف حتى 150 MiB، دون حد للصفحات.
- الأرشفة تحذف ملف PDF الأصلي فقط من Storage لتفريغ المساحة. لا تحذف سجل الكتاب أو البصمة أو الغلاف أو المخرجات المدفوعة؛ عدد الكتب المؤرشفة لا يخضع لحد الستة.
- لاستعادة الأصل: إخلاء مكان ضمن الستة بأرشفة كتاب آخر، ثم إعادة رفع الملف نفسه. تُستخدم بصمة SHA-256 لاستعادة السجل نفسه ومخرجاته السابقة دون تكرار أو طلب دفع جديد مقابل المخرجات المحفوظة.
- لا تعرض واجهة الأرشيف خيار حذف النتائج نهائيًا.

## التنفيذ

- `src/lib/bookUpload.ts`: نقل TUS بقطع 6 MiB، تقدم فعلي، إعادة محاولة الأجزاء المتعثرة ضمن المحاولة الجارية، وإيقاف النقل إذا انعدم التقدم 3 دقائق. لا يستأنف تلقائيًا بعد إغلاق الصفحة.
- `src/lib/library.ts`: تحرير PDF worker وBlob URL قبل حساب البصمة، تحديد مهل للفحص والطلبات، واستعادة الأرشيف بالنقل المجزأ. أخطاء الاستعادة لا تسقط إلى إنشاء كتاب جديد. لا يُحذف الأصل تلقائيًا عندما تكون نتيجة إدخال السجل غير مؤكدة بسبب انقطاع الرد.
- `src/App.tsx`, `src/v0103.css`: مراحل واضحة، نسبة نقل حقيقية، خطأ ثابت في نافذة الرفع، منع النقر المكرر، إزالة الحذف النهائي للأرشيف، وعدم الادعاء بتحرير المساحة إذا لم يتأكد حذف الأصل.
- `public/sw.js`: تحديث مراجعة الكاش. تحديث اختباري الكاش/الأرشيف في `scripts/verify-v0104-library.mjs` و`scripts/verify-v0191-index-exports.mjs`.
- `package.json`, `package-lock.json`: تثبيت `tus-js-client` عند 4.3.1.
- `scripts/test-book-upload.cjs`: اختبارات متصفح مع اعتراض جميع طلبات Auth/Database/Storage. لا تدخل إلى البيانات الحقيقية ولا تستدعي AI.

## التحقق المنفّذ

- `npm run build`: ناجح.
- `npm test`: ناجح، جميع مجموعات الاختبار الحالية.
- `npm audit --audit-level=high`: صفر ثغرات.
- Chromium، عرض 390×844، باستخدام ملف «قصتي مع النوم» الحقيقي: 101,968,893 بايت، 244 صفحة. نجح الفحص والنقل المجزأ، وتطابقت SHA-256 للبايتات المنقولة، وتحرر عامل PDF.
- محاكاة 503 أثناء رفع جزء: نجح الاستكمال عبر HEAD دون إعادة إرسال الملف كله.
- نجح فتح النسخة المكررة دون رفع، واستعادة المؤرشف بالسجل نفسه والمخرجات نفسها دون INSERT جديد.
- نجح منع الاستعادة عند اكتمال الستة، ومنع إنشاء نسخة جديدة عند رفض الاستعادة، ورفض ملف يتجاوز 150 MiB، وإيقاف اتصال متعطل.
- قراءة فقط من Supabase: حاوية `spl-books` خاصة وحدها 157286400 بايت. الأصل المؤرشف «التحول الرقمي في الإدارة والقيادة الحديثة» محذوف فعليًا؛ بصمته ونتيجة تحليل واحدة و3 تسجيلات صوتية باقية.

## حدود التحقق والخطوة التالية

لا يوجد اختبار رفع حي بحساب المستخدم من هذه الجلسة، ولم تُختبر أجهزة iPhone/Samsung الحقيقية. تعذر تجهيز WebKit بسبب تعذر تنزيل المحرك؛ اختبار Chromium استخدم محركًا بديلًا مؤقتًا خارج المشروع. لم تُغير الجداول أو RLS أو بيانات المستخدم، ولم تُشغّل خدمة مدفوعة.

الخطوة التالية بعد مراجعة فرع المعاينة: اعتماد الدمج والنشر، ثم تجربة الملف نفسه من الهاتف. لا تُعتبر نتيجة الاختبار المحاكى تأكيدًا لنجاح الرفع في بيئة الإنتاج.

لإعادة اختبار المتصفح، وفّر Playwright ومحركاته ثم شغّل Vite بمتغيرات اختبار محلية فقط:

```sh
VITE_SUPABASE_URL=http://127.0.0.1:4179 VITE_SUPABASE_PUBLISHABLE_KEY=test-public-key npm run dev -- --host 127.0.0.1 --port 4179
SPL_TEST_PDF=/absolute/path/to/test.pdf node scripts/test-book-upload.cjs
```

يمكن اختيار Chromium فقط بـ`SPL_TEST_BROWSER=chromium` أو تحديد مسار محركه بـ`SPL_CHROMIUM_PATH`. عند عدم تقديم ملف يُنشئ الاختبار PDF من صفـحة واحدة يتجاوز حجم القطعة للاختبار؛ لا يُحفظ كتاب المستخدم في المستودع.
