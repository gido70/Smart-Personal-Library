# ملاحظة الـ Migration — V0.7.3-candidate

**لم تُنفَّذ أي Migration في هذه الجلسة، ولن تُنفَّذ إلا بعد إذنك الصريح.**

## القرار: لا حاجة لـ Migration جديدة

راجعت كل إصلاح في V0.7.3 (انظر `CLAUDE-AUDIT-REPORT.md`) بحثًا عن حاجة لتغيير مخطط قاعدة البيانات، والنتيجة أن لا شيء منها يحتاج ذلك:

- تعبئة `source_language`/`title` الرجعية تكتب إلى عمودين موجودين أصلًا منذ مخطط V0.5 (`20260825_0001_spl_v05_pilot.sql`).
- نص صفحات البحث المحلي (`pages_text`) يُخزَّن داخل عمود `content` من نوع `jsonb` الموجود أصلًا في `spl_analyses` — لا عمود جديد.
- مراجعة التكرارات تعتمد على `content_sha256` الذي أضافته Migration الـ V0.7 (`20260826_0002_spl_v07_zero_cost.sql`). الملف المرفق مطابق حرفيًا للملف الذي نُفذ سابقًا بنجاح في مشروع Supabase الفعلي، ولذلك لا يُعاد تنفيذه الآن.
- ترقية الحساب الدائم تستخدم آلية Supabase Auth المدمجة (`auth.updateUser`)، ولا تحتاج جدول أو عمود جديد على الإطلاق.

## ما هو مرفق في هذا المجلد إذن؟

نسخة **غير مُعدَّلة** من `supabase/migrations/20260826_0002_spl_v07_zero_cost.sql` — نفس الملف الذي استلمته ضمن حزمة V0.7.2 ونُفذ سابقًا. لا توجد Migration جديدة مطلوبة لـV0.7.3.
