# V0.11 — ملاحظات ما قبل الدمج

- لا دمج تلقائي إلى `main`.
- لا تشغيل OpenAI مدفوع أثناء البناء.
- كل تغيير V0.11 موجود في فرع مستقل.
- الواجهة القديمة ما زالت متاحة داخل المعاينة عبر `?legacy=1` للمقارنة والرجوع.
- Migration V0.11 إضافية فقط ولا تحذف أو تعيد تسمية أي كائن سابق.
- Web Push لا يعتبر مفعلاً حتى إضافة VAPID secrets وجدولة `spl-reminders` واختبار جهاز حقيقي.
- الصوت المدفوع لا يعتبر معتمداً قبل اختبار قبول عربي فعلي.
- المراجعة التالية: GitHub Actions ثم مراجعة التغييرات ثم معاينة جهاز حقيقي.
